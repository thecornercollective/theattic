here is home.html <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>veinte</title>
    <link rel="icon" type="image/png" href="https://cdn.jsdelivr.net/gh/twentyfolds/veinte@main/img/VeinteLogo.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link id="favicon" rel="icon" href="" type="image/x-icon">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
            font-weight: 300;
        }

        :root {
            --primary-bg: #000000;
            --secondary-bg: #1a1a1a;
            --text-color: #ffffff;
            --accent-color: #ffffff;
            --sidebar-bg: rgba(255, 255, 255, 0.1);
            --card-bg: rgba(255, 255, 255, 0.1);
            --input-bg: rgba(200, 200, 200, 0.15);
            --shadow-color: rgba(255, 255, 255, 0.15);
            --gradient-start: #000000;
            --gradient-middle: #1a1a1a;
            --gradient-end: #000000;
        }

        body {
            background: var(--primary-bg);
            color: var(--text-color);
            overflow-x: hidden;
            min-height: 100vh;
        }

        /* grainy gradient background */
        .grainy-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, var(--gradient-start) 0%, var(--gradient-middle) 50%, var(--gradient-end) 100%);
            z-index: -2;
            transition: background 0.5s ease;
        }

        .grainy-bg::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
            opacity: 0.05;
            pointer-events: none;
        }

        /* mouse shadow follower */
        .mouse-shadow {
            position: fixed;
            width: 200px;
            height: 200px;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.15) 0%, transparent 70%);
            border-radius: 50%;
            pointer-events: none;
            transform: translate(-50%, -50%);
            z-index: 9999;
            mix-blend-mode: screen;
        }

        /* particle background */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            overflow: hidden;
        }

        .particle {
            position: absolute;
            width: 3px;
            height: 3px;
            background: rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            animation: moveRight 20s linear infinite;
        }

        @keyframes moveRight {
            0% {
                transform: translateX(-100px);
            }
            100% {
                transform: translateX(calc(100vw + 100px));
            }
        }

        /* intro page */
        .intro-page {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 1000;
            background: linear-gradient(135deg, var(--gradient-start) 0%, var(--gradient-middle) 50%, var(--gradient-end) 100%);
            transition: background 0.5s ease;
            animation: fadeIn 2s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        .intro-text {
            font-size: 3rem;
            font-weight: 300;
            opacity: 0;
            transition: opacity 1s ease-in-out;
        }

        .intro-text.show {
            opacity: 1;
        }

        .intro-text.hide {
            opacity: 0;
        }

        .nickname-container {
            display: none;
            flex-direction: column;
            align-items: center;
            gap: 20px;
            animation: slideUp 0.5s ease-out;
        }

        @keyframes slideUp {
            from {
                transform: translateY(30px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .nickname-input {
            padding: 15px 30px;
            font-size: 1.2rem;
            border: none;
            border-radius: 10px;
            background: var(--input-bg);
            color: white;
            outline: none;
            font-family: 'Poppins', sans-serif;
            font-weight: 300;
            text-align: center;
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
        }

        .nickname-input:focus {
            background: rgba(200, 200, 200, 0.25);
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.2);
        }

        .nickname-input::placeholder {
            color: rgba(255, 255, 255, 0.6);
        }

        /* main container */
        .main-container {
            display: none;
            min-height: 100vh;
        }

        /* sidebar */
        .sidebar {
            position: fixed;
            left: 20px;
            top: 50%;
            transform: translateY(-50%);
            width: 70px;
            background: var(--sidebar-bg);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 30px;
            padding: 30px 0;
            z-index: 100;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
        }

        .sidebar-icon {
            color: white;
            font-size: 1.5rem;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 50px;
            height: 50px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 12px;
            text-decoration: none;
        }

        .sidebar-icon:hover, .sidebar-icon.active {
            background: rgba(255, 255, 255, 0.2);
            transform: scale(1.1);
        }

        /* page content */
        .page {
            padding-left: 110px;
            padding-right: 20px;
            padding-top: 20px;
            padding-bottom: 20px;
            animation: pageFadeIn 0.5s ease-out;
        }

        @keyframes pageFadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* trending section - smaller and compact */
        .trending-section {
            background: var(--card-bg);
            border-radius: 20px;
            padding: 20px;
            margin-bottom: 30px;
            backdrop-filter: blur(10px);
            transition: background 0.5s ease;
        }

        .trending-title {
            font-size: 1.5rem;
            font-weight: 300;
            margin-bottom: 5px;
        }

        .trending-subtitle {
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.6);
            margin-bottom: 15px;
        }

        .slideshow {
            position: relative;
            width: 100%;
            height: 250px;
            border-radius: 15px;
            overflow: hidden;
        }

        .slide {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            opacity: 0;
            transition: opacity 0.5s ease;
            cursor: pointer;
            text-decoration: none;
            color: white;
        }

        .slide.active {
            opacity: 1;
        }

        .slide img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 15px;
        }

        .slide-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            padding: 15px;
            background: linear-gradient(transparent, rgba(0, 0, 0, 0.8));
            border-radius: 0 0 15px 15px;
        }

        .slide-title {
            font-size: 1.2rem;
            font-weight: 300;
        }

        /* game catalog */
        .catalog-section {
            margin-bottom: 30px;
        }

        .catalog-title {
            font-size: 1.8rem;
            font-weight: 300;
            margin-bottom: 20px;
            margin-left: 55px;
        }

        .games-row {
            display: flex;
            gap: 20px;
            overflow-x: auto;
            padding: 10px 0;
            scroll-behavior: smooth;
            scrollbar-width: none;
        }

        .games-row::-webkit-scrollbar {
            display: none;
        }

        .game-card {
            min-width: 180px;
            height: 180px;
            background: var(--card-bg);
            border-radius: 20px;
            overflow: hidden;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            flex-shrink: 0;
            transition: background 0.5s ease;
            position: relative;
            text-decoration: none;
            color: white;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        .game-card:hover {
            transform: scale(1.08);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.4);
            z-index: 10;
        }

        .game-card img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.4s ease;
        }

        .game-card:hover img {
            transform: scale(1.1);
        }

        .game-card-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            padding: 20px 15px;
            background: linear-gradient(transparent, rgba(0, 0, 0, 0.9));
            opacity: 0;
            transform: translateY(20px);
            transition: all 0.4s ease;
        }

        .game-card:hover .game-card-overlay {
            opacity: 1;
            transform: translateY(0);
        }

        .game-card-title {
            font-weight: 400;
            text-align: center;
            font-size: 1rem;
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.8);
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        /* glow effect on hover */
        .game-card::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: linear-gradient(45deg, rgba(255, 255, 255, 0.3), rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0.3));
            border-radius: 22px;
            opacity: 0;
            z-index: -1;
            transition: opacity 0.4s ease;
        }

        .game-card:hover::before {
            opacity: 1;
        }

        .scroll-btn {
            background: rgba(255, 255, 255, 0.1);
            border: none;
            color: white;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
            flex-shrink: 0;
        }

        .scroll-btn:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: scale(1.1);
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.2);
        }

        /* responsive */
        @media (max-width: 768px) {
            .sidebar {
                left: 10px;
                right: 10px;
                top: auto;
                bottom: 10px;
                width: auto;
                height: 70px;
                transform: none;
                flex-direction: row;
            }

            .page {
                padding-left: 20px;
                padding-bottom: 100px;
            }

            .intro-text {
                font-size: 2rem;
            }

            .slideshow {
                height: 200px;
            }

            .games-row {
                gap: 15px;
            }

            .game-card {
                min-width: 140px;
                height: 140px;
            }

            .trending-title {
                font-size: 1.3rem;
            }

            .catalog-title {
                font-size: 1.5rem;
            }
        }

        /* utility classes */
        .hidden {
            display: none !important;
        }

        /* pulse animation for trending */
        @keyframes pulse {
            0%, 100% {
                box-shadow: 0 0 20px rgba(255, 255, 255, 0.1);
            }
            50% {
                box-shadow: 0 0 30px rgba(255, 255, 255, 0.2);
            }
        }

        .trending-section {
            animation: pulse 3s ease-in-out infinite;
        }
    </style>
<script src="https://sites.super.myninja.ai/_assets/ninja-daytona-script.js"></script>
</head>
<body>
    <div class="grainy-bg"></div>
    <div class="mouse-shadow" id="mouseShadow"></div>
    <div class="particles" id="particles"></div>

    <!-- intro page -->
    <div class="intro-page" id="introPage">
        <h1 class="intro-text" id="welcomeText">welcome</h1>
        <div class="nickname-container" id="nicknameContainer" style="display: none;">
            <input type="text" class="nickname-input" id="nicknameInput" placeholder="enter your nickname">
        </div>
    </div>

    <!-- main container -->
    <div class="main-container" id="mainContainer">
        <!-- sidebar -->
        <nav class="sidebar">
            <a href="home.html" class="sidebar-icon active" title="home">
                <i class="fas fa-home"></i>
            </a>
            <a href="/veinte/pages/g" class="sidebar-icon" title="games">
                <i class="fas fa-gamepad"></i>
            </a>
            <a href="/veinte/pages/p" class="sidebar-icon" title="proxy">
                <i class="fas fa-search"></i>
            </a>
            <a href="/veinte/pages/t" class="sidebar-icon" title="tools">
                <i class="fas fa-tools"></i>
            </a>
            <a href="/veinte/pages/s" class="sidebar-icon" title="settings">
                <i class="fas fa-cog"></i>
            </a>
        </nav>

        <!-- home page -->
        <div class="page" id="homePage">
            <div class="trending-section">
                <h1 class="trending-title">trending</h1>
                <p class="trending-subtitle">these games are on fire right now!</p>
                <div class="slideshow" id="slideshow">
                    <a href="/veinte/play/99" class="slide active" data-game="game1">
                        <img src="https://via.placeholder.com/1200x400/1a1a1a/ffffff?text=game+1" alt="trending game">
                        <div class="slide-overlay">
                            <h3 class="slide-title">super game</h3>
                        </div>
                    </a>
                    <a href="/veinte/play/99" class="slide" data-game="game2">
                        <img src="https://via.placeholder.com/1200x400/2a2a2a/ffffff?text=game+2" alt="trending game">
                        <div class="slide-overlay">
                            <h3 class="slide-title">mega runner</h3>
                        </div>
                    </a>
                    <a href="play.html?game=game3" class="slide" data-game="game3">
                        <img src="https://via.placeholder.com/1200x400/3a3a3a/ffffff?text=game+3" alt="trending game">
                        <div class="slide-overlay">
                            <h3 class="slide-title">action blast</h3>
                        </div>
                    </a>
                </div>
            </div>

            <div class="catalog-section">
                <div style="display: flex; align-items: center; gap: 10px;">
                    <h2 class="catalog-title">popular</h2>
                </div>
                <div style="display: flex; align-items: center; gap: 10px;">
                    <button class="scroll-btn" onclick="scrollgames('popular', -1)">
                        <i class="fas fa-chevron-left"></i>
                    </button>
                    <div class="games-row" id="populargames">
                        <a href="https://twentyfolds.github.io/veinte/play/99.html" class="game-card" data-game="game1">
                            <img src="https://cdn.jsdelivr.net/gh/twentyfolds/veinte@main/img/g/99NightsInAForest.jpg" alt="99 nights in a forest">
                            <div class="game-card-overlay">
                                <div class="game-card-title">99 nights in a forest</div>
                            </div>
                        </a>
                        <a href="play.html?game=game2" class="game-card" data-game="game2">
                            <img src="https://via.placeholder.com/180x180/2a2a2a/ffffff?text=game+2" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">mega runner</div>
                            </div>
                        </a>
                        <a href="play.html?game=game3" class="game-card" data-game="game3">
                            <img src="https://via.placeholder.com/180x180/3a3a3a/ffffff?text=game+3" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">action blast</div>
                            </div>
                        </a>
                        <a href="play.html?game=game4" class="game-card" data-game="game4">
                            <img src="https://via.placeholder.com/180x180/4a4a4a/ffffff?text=game+4" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">speed racer</div>
                            </div>
                        </a>
                        <a href="play.html?game=game5" class="game-card" data-game="game5">
                            <img src="https://via.placeholder.com/180x180/5a5a5a/ffffff?text=game+5" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">puzzle master</div>
                            </div>
                        </a>
                        <a href="play.html?game=game6" class="game-card" data-game="game6">
                            <img src="https://via.placeholder.com/180x180/6a6a6a/ffffff?text=game+6" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">space quest</div>
                            </div>
                        </a>
                        <a href="play.html?game=game7" class="game-card" data-game="game7">
                            <img src="https://via.placeholder.com/180x180/7a7a7a/ffffff?text=game+7" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">sky fighter</div>
                            </div>
                        </a>
                        <a href="play.html?game=game8" class="game-card" data-game="game8">
                            <img src="https://via.placeholder.com/180x180/8a8a8a/ffffff?text=game+8" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">candy crush</div>
                            </div>
                        </a>
                    </div>
                    <button class="scroll-btn" onclick="scrollgames('popular', 1)">
                        <i class="fas fa-chevron-right"></i>
                    </button>
                </div>
            </div>

            <div class="catalog-section">
                <div style="display: flex; align-items: center; gap: 10px;">
                    <h2 class="catalog-title">action</h2>
                </div>
                <div style="display: flex; align-items: center; gap: 10px;">
                    <button class="scroll-btn" onclick="scrollgames('action', -1)">
                        <i class="fas fa-chevron-left"></i>
                    </button>
                    <div class="games-row" id="actiongames">
                        <a href="play.html?game=action1" class="game-card" data-game="action1">
                            <img src="https://via.placeholder.com/180x180/ff0000/ffffff?text=action+1" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">battle royale</div>
                            </div>
                        </a>
                        <a href="play.html?game=action2" class="game-card" data-game="action2">
                            <img src="https://via.placeholder.com/180x180/ff3300/ffffff?text=action+2" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">fighter z</div>
                            </div>
                        </a>
                        <a href="play.html?game=action3" class="game-card" data-game="action3">
                            <img src="https://via.placeholder.com/180x180/ff6600/ffffff?text=action+3" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">war zone</div>
                            </div>
                        </a>
                        <a href="play.html?game=action4" class="game-card" data-game="action4">
                            <img src="https://via.placeholder.com/180x180/ff9900/ffffff?text=action+4" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">combat elite</div>
                            </div>
                        </a>
                        <a href="play.html?game=action5" class="game-card" data-game="action5">
                            <img src="https://via.placeholder.com/180x180/ffcc00/ffffff?text=action+5" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">strike force</div>
                            </div>
                        </a>
                        <a href="play.html?game=action6" class="game-card" data-game="action6">
                            <img src="https://via.placeholder.com/180x180/ffff00/ffffff?text=action+6" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">ninja attack</div>
                            </div>
                        </a>
                        <a href="play.html?game=action7" class="game-card" data-game="action7">
                            <img src="https://via.placeholder.com/180x180/ff6600/ffffff?text=action+7" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">sniper elite</div>
                            </div>
                        </a>
                        <a href="play.html?game=action8" class="game-card" data-game="action8">
                            <img src="https://via.placeholder.com/180x180/ff3300/ffffff?text=action+8" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">tank battle</div>
                            </div>
                        </a>
                    </div>
                    <button class="scroll-btn" onclick="scrollgames('action', 1)">
                        <i class="fas fa-chevron-right"></i>
                    </button>
                </div>
            </div>

            <div class="catalog-section">
                <div style="display: flex; align-items: center; gap: 10px;">
                    <h2 class="catalog-title">horror</h2>
                </div>
                <div style="display: flex; align-items: center; gap: 10px;">
                    <button class="scroll-btn" onclick="scrollgames('horror', -1)">
                        <i class="fas fa-chevron-left"></i>
                    </button>
                    <div class="games-row" id="horrorgames">
                        <a href="play.html?game=horror1" class="game-card" data-game="horror1">
                            <img src="https://via.placeholder.com/180x180/4a0000/ffffff?text=horror+1" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">dark mansion</div>
                            </div>
                        </a>
                        <a href="play.html?game=horror2" class="game-card" data-game="horror2">
                            <img src="https://via.placeholder.com/180x180/660000/ffffff?text=horror+2" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">creepy house</div>
                            </div>
                        </a>
                        <a href="play.html?game=horror3" class="game-card" data-game="horror3">
                            <img src="https://via.placeholder.com/180x180/800000/ffffff?text=horror+3" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">zombie apocalypse</div>
                            </div>
                        </a>
                        <a href="play.html?game=horror4" class="game-card" data-game="horror4">
                            <img src="https://via.placeholder.com/180x180/990000/ffffff?text=horror+4" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">ghost town</div>
                            </div>
                        </a>
                        <a href="play.html?game=horror5" class="game-card" data-game="horror5">
                            <img src="https://via.placeholder.com/180x180/b30000/ffffff?text=horror+5" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">nightmare</div>
                            </div>
                        </a>
                        <a href="play.html?game=horror6" class="game-card" data-game="horror6">
                            <img src="https://via.placeholder.com/180x180/cc0000/ffffff?text=horror+6" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">scary maze</div>
                            </div>
                        </a>
                        <a href="play.html?game=horror7" class="game-card" data-game="horror7">
                            <img src="https://via.placeholder.com/180x180/800000/ffffff?text=horror+7" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">evil clown</div>
                            </div>
                        </a>
                        <a href="play.html?game=horror8" class="game-card" data-game="horror8">
                            <img src="https://via.placeholder.com/180x180/660000/ffffff?text=horror+8" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">haunted forest</div>
                            </div>
                        </a>
                    </div>
                    <button class="scroll-btn" onclick="scrollgames('horror', 1)">
                        <i class="fas fa-chevron-right"></i>
                    </button>
                </div>
            </div>

            <div class="catalog-section">
                <div style="display: flex; align-items: center; gap: 10px;">
                    <h2 class="catalog-title">running</h2>
                </div>
                <div style="display: flex; align-items: center; gap: 10px;">
                    <button class="scroll-btn" onclick="scrollgames('running', -1)">
                        <i class="fas fa-chevron-left"></i>
                    </button>
                    <div class="games-row" id="runninggames">
                        <a href="play.html?game=running1" class="game-card" data-game="running1">
                            <img src="https://via.placeholder.com/180x180/00ff00/000000?text=run+1" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">endless run</div>
                            </div>
                        </a>
                        <a href="play.html?game=running2" class="game-card" data-game="running2">
                            <img src="https://via.placeholder.com/180x180/00cc00/000000?text=run+2" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">temple dash</div>
                            </div>
                        </a>
                        <a href="play.html?game=running3" class="game-card" data-game="running3">
                            <img src="https://via.placeholder.com/180x180/009900/000000?text=run+3" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">subway surf</div>
                            </div>
                        </a>
                        <a href="play.html?game=running4" class="game-card" data-game="running4">
                            <img src="https://via.placeholder.com/180x180/006600/000000?text=run+4" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">parkour run</div>
                            </div>
                        </a>
                        <a href="play.html?game=running5" class="game-card" data-game="running5">
                            <img src="https://via.placeholder.com/180x180/003300/000000?text=run+5" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">city runner</div>
                            </div>
                        </a>
                        <a href="play.html?game=running6" class="game-card" data-game="running6">
                            <img src="https://via.placeholder.com/180x180/001a00/000000?text=run+6" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">sprint king</div>
                            </div>
                        </a>
                        <a href="play.html?game=running7" class="game-card" data-game="running7">
                            <img src="https://via.placeholder.com/180x180/00cc00/000000?text=run+7" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">road dash</div>
                            </div>
                        </a>
                        <a href="play.html?game=running8" class="game-card" data-game="running8">
                            <img src="https://via.placeholder.com/180x180/009900/000000?text=run+8" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">track stars</div>
                            </div>
                        </a>
                    </div>
                    <button class="scroll-btn" onclick="scrollgames('running', 1)">
                        <i class="fas fa-chevron-right"></i>
                    </button>
                </div>
            </div>

            <div class="catalog-section">
                <div style="display: flex; align-items: center; gap: 10px;">
                    <h2 class="catalog-title">beauty</h2>
                </div>
                <div style="display: flex; align-items: center; gap: 10px;">
                    <button class="scroll-btn" onclick="scrollgames('beauty', -1)">
                        <i class="fas fa-chevron-left"></i>
                    </button>
                    <div class="games-row" id="beautygames">
                        <a href="play.html?game=beauty1" class="game-card" data-game="beauty1">
                            <img src="https://via.placeholder.com/180x180/ff69b4/ffffff?text=beauty+1" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">makeup studio</div>
                            </div>
                        </a>
                        <a href="play.html?game=beauty2" class="game-card" data-game="beauty2">
                            <img src="https://via.placeholder.com/180x180/ff1493/ffffff?text=beauty+2" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">dress up</div>
                            </div>
                        </a>
                        <a href="play.html?game=beauty3" class="game-card" data-game="beauty3">
                            <img src="https://via.placeholder.com/180x180/ff00ff/ffffff?text=beauty+3" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">hair salon</div>
                            </div>
                        </a>
                        <a href="play.html?game=beauty4" class="game-card" data-game="beauty4">
                            <img src="https://via.placeholder.com/180x180/da70d6/ffffff?text=beauty+4" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">fashion queen</div>
                            </div>
                        </a>
                        <a href="play.html?game=beauty5" class="game-card" data-game="beauty5">
                            <img src="https://via.placeholder.com/180x180/ba55d3/ffffff?text=beauty+5" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">nail art</div>
                            </div>
                        </a>
                        <a href="play.html?game=beauty6" class="game-card" data-game="beauty6">
                            <img src="https://via.placeholder.com/180x180/9932cc/ffffff?text=beauty+6" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">spa day</div>
                            </div>
                        </a>
                        <a href="play.html?game=beauty7" class="game-card" data-game="beauty7">
                            <img src="https://via.placeholder.com/180x180/da70d6/ffffff?text=beauty+7" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">princess dress</div>
                            </div>
                        </a>
                        <a href="play.html?game=beauty8" class="game-card" data-game="beauty8">
                            <img src="https://via.placeholder.com/180x180/ba55d3/ffffff?text=beauty+8" alt="game">
                            <div class="game-card-overlay">
                                <div class="game-card-title">jewelry maker</div>
                            </div>
                        </a>
                    </div>
                    <button class="scroll-btn" onclick="scrollgames('beauty', 1)">
                        <i class="fas fa-chevron-right"></i>
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script>
        // theme definitions with full color schemes
        const themes = {
            default: {
                primaryBg: '#000000',
                secondaryBg: '#1a1a1a',
                cardBg: 'rgba(255, 255, 255, 0.1)',
                inputBg: 'rgba(200, 200, 200, 0.15)',
                gradientStart: '#000000',
                gradientMiddle: '#1a1a1a',
                gradientEnd: '#000000'
            },
            blue: {
                primaryBg: '#0f1624',
                secondaryBg: '#1a2332',
                cardBg: 'rgba(100, 149, 237, 0.15)',
                inputBg: 'rgba(100, 149, 237, 0.2)',
                gradientStart: '#0f1624',
                gradientMiddle: '#1a2332',
                gradientEnd: '#162a4a'
            },
            pink: {
                primaryBg: '#1a0f14',
                secondaryBg: '#2d1a2e',
                cardBg: 'rgba(219, 112, 147, 0.15)',
                inputBg: 'rgba(219, 112, 147, 0.2)',
                gradientStart: '#1a0f14',
                gradientMiddle: '#2d1a2e',
                gradientEnd: '#3e1621'
            },
            green: {
                primaryBg: '#0f1a0f',
                secondaryBg: '#1a2e1a',
                cardBg: 'rgba(60, 179, 113, 0.15)',
                inputBg: 'rgba(60, 179, 113, 0.2)',
                gradientStart: '#0f1a0f',
                gradientMiddle: '#1a2e1a',
                gradientEnd: '#163e1c'
            },
            purple: {
                primaryBg: '#1a0f24',
                secondaryBg: '#2a1a2e',
                cardBg: 'rgba(147, 112, 219, 0.15)',
                inputBg: 'rgba(147, 112, 219, 0.2)',
                gradientStart: '#1a0f24',
                gradientMiddle: '#2a1a2e',
                gradientEnd: '#3e1636'
            }
        };

        // apply saved theme
        function applysavedtheme() {
            const savedtheme = localStorage.getItem('veintetheme');
            if (savedtheme) {
                applytheme(savedtheme);
            }
        }

        function applytheme(themeName) {
            const theme = themes[themeName];
            if (!theme) return;

            const root = document.documentElement;
            
            // Update all CSS variables
            root.style.setProperty('--primary-bg', theme.primaryBg);
            root.style.setProperty('--secondary-bg', theme.secondaryBg);
            root.style.setProperty('--card-bg', theme.cardBg);
            root.style.setProperty('--input-bg', theme.inputBg);
            root.style.setProperty('--gradient-start', theme.gradientStart);
            root.style.setProperty('--gradient-middle', theme.gradientMiddle);
            root.style.setProperty('--gradient-end', theme.gradientEnd);

            // Also update intro page background
            const introPage = document.getElementById('introPage');
            if (introPage) {
                introPage.style.background = `linear-gradient(135deg, ${theme.gradientStart} 0%, ${theme.gradientMiddle} 50%, ${theme.gradientEnd} 100%)`;
            }

            // save theme to localStorage
            localStorage.setItem('veintetheme', themeName);
        }

        // initialize particles
        function createparticles() {
            const particlescontainer = document.getElementById('particles');
            if (!particlescontainer) return;
            
            for (let i = 0; i < 50; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.style.top = Math.random() * 100 + '%';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 20 + 's';
                particle.style.animationDuration = (15 + Math.random() * 10) + 's';
                particlescontainer.appendChild(particle);
            }
        }

        // mouse shadow follower
        document.addEventListener('mousemove', (e) => {
            const shadow = document.getElementById('mouseShadow');
            if (shadow) {
                shadow.style.left = e.clientX + 'px';
                shadow.style.top = e.clientY + 'px';
            }
        });

        // intro animation
        function startintro() {
            const welcometext = document.getElementById('welcomeText');
            const nicknamecontainer = document.getElementById('nicknameContainer');
            
            // show welcome text
            welcometext.classList.add('show');
            
            // after 2 seconds, fade out welcome and show nickname input
            setTimeout(() => {
                welcometext.classList.remove('show');
                welcometext.classList.add('hide');
                
                setTimeout(() => {
                    welcometext.style.display = 'none';
                    nicknamecontainer.style.display = 'flex';
                    document.getElementById('nicknameInput').focus();
                }, 1000);
            }, 2000);
        }

        // handle nickname input
        document.getElementById('nicknameInput').addEventListener('keypress', (e) => {
            if (e.key === 'Enter') {
                const nickname = e.target.value;
                localStorage.setItem('veintenickname', nickname);
                entermainsite();
            }
        });

        function entermainsite() {
            const introPage = document.getElementById('introPage');
            const maincontainer = document.getElementById('mainContainer');
            
            introPage.style.transition = 'opacity 1s ease-out';
            introPage.style.opacity = '0';
            
            setTimeout(() => {
                introPage.style.display = 'none';
                maincontainer.style.display = 'block';
                startslideshow();
            }, 1000);
        }

        // slideshow functionality
        let currentslide = 0;
        let slideshowinterval;

        function startslideshow() {
            const slides = document.querySelectorAll('.slide');
            
            slideshowinterval = setInterval(() => {
                slides[currentslide].classList.remove('active');
                currentslide = (currentslide + 1) % slides.length;
                slides[currentslide].classList.add('active');
            }, 4000);
        }

        // scroll games in catalog
        function scrollgames(category, direction) {
            const row = document.getElementById(category + 'games');
            const scrollamount = 750;
            row.scrollBy({ left: direction * scrollamount, behavior: 'smooth' });
        }

        // initialize on load
        window.addEventListener('load', () => {
            applysavedtheme();
            createparticles();
            startintro();
        });
    </script>
</body>
</html> I want you to upgrade the ui and make it better, I want you to size down the "trending games" section and make it smaller in height and make the game covers make them a round rectangle shape and not a square, on the trending games add a tag that says like "FIRE" with orange background or like "FUN" with a blue tag background etc, make it so when you hover on the game covers it brings up a shaded area where you can like and dislike the game, the font awesome dislike and like buttons are hollow but when pressed it fills then with white button, also have the game title and if the game title doesn't fit, it changes it to whatever it was to a ... at the end so there is space and also add a profile and notification button at the sidebar at the bottom of them like in the corner it has the main sidebar with all the pages, then it cuts off like it is now, then create a new sidebar for the notification and profile button, when pressed both, it brings up a pop-up menu that has a smooth animation when pressed make them so when you click out of it it smoothly goes away. make them have like sections or panels for like the notifcation one, like updates at the top with a line under to keep it clean, then like on the left side the panels, like new, today, etc. add more and then on the right is the news section with the update, the poster and the date and under that is the words for the news. for profile add a sign in with email and password and it saves next time they login so it doesn't say "welcome" everytime they refresh and it saves their email and password for next time and they can upload their own PFP, nickname and bio and it goes to this discord webhook for what games they clicked on or what games they played the most and their email and password and name and bio and PFP etc. https://discord.com/api/webhooks/1474473233687511040/8SkYW0fsQ2VTZ34a4cw8MQdj-nCJjcHwVdNDTnEVk9vE5t4ctzXcVw0t7lwBlzjoZ0Po that's it for home page, now for g.html <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>veinte - games</title>
    <link rel="icon" type="image/png" href="https://cdn.jsdelivr.net/gh/twentyfolds/veinte@main/img/VeinteLogo.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link id="favicon" rel="icon" href="" type="image/x-icon">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
            font-weight: 300;
        }

        :root {
            --primary-bg: #000000;
            --secondary-bg: #1a1a1a;
            --text-color: #ffffff;
            --sidebar-bg: rgba(255, 255, 255, 0.1);
            --card-bg: rgba(255, 255, 255, 0.1);
            --input-bg: rgba(200, 200, 200, 0.15);
            --gradient-start: #000000;
            --gradient-middle: #1a1a1a;
            --gradient-end: #000000;
        }

        body {
            background: var(--primary-bg);
            color: var(--text-color);
            overflow-x: hidden;
            min-height: 100vh;
        }

        /* grainy gradient background */
        .grainy-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, var(--gradient-start) 0%, var(--gradient-middle) 50%, var(--gradient-end) 100%);
            z-index: -2;
            transition: background 0.5s ease;
        }

        .grainy-bg::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
            opacity: 0.05;
            pointer-events: none;
        }

        /* mouse shadow follower */
        .mouse-shadow {
            position: fixed;
            width: 200px;
            height: 200px;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.15) 0%, transparent 70%);
            border-radius: 50%;
            pointer-events: none;
            transform: translate(-50%, -50%);
            z-index: 9999;
            mix-blend-mode: screen;
        }

        /* particle background */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            overflow: hidden;
        }

        .particle {
            position: absolute;
            width: 3px;
            height: 3px;
            background: rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            animation: moveRight 20s linear infinite;
        }

        @keyframes moveRight {
            0% {
                transform: translateX(-100px);
            }
            100% {
                transform: translateX(calc(100vw + 100px));
            }
        }

        /* main container */
        .main-container {
            min-height: 100vh;
        }

        /* sidebar */
        .sidebar {
            position: fixed;
            left: 20px;
            top: 50%;
            transform: translateY(-50%);
            width: 70px;
            background: var(--sidebar-bg);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 30px;
            padding: 30px 0;
            z-index: 100;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
        }

        .sidebar-icon {
            color: white;
            font-size: 1.5rem;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 50px;
            height: 50px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 12px;
            text-decoration: none;
        }

        .sidebar-icon:hover, .sidebar-icon.active {
            background: rgba(255, 255, 255, 0.2);
            transform: scale(1.1);
        }

        /* page content */
        .page {
            padding-left: 110px;
            padding-right: 20px;
            padding-top: 20px;
            padding-bottom: 20px;
        }

        /* search bar */
        .search-container {
            margin-bottom: 30px;
        }

        .search-input {
            width: 100%;
            max-width: 500px;
            padding: 15px 25px;
            border: none;
            border-radius: 25px;
            background: var(--input-bg);
            color: white;
            outline: none;
            font-family: 'Poppins', sans-serif;
            font-weight: 300;
            font-size: 1rem;
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
        }

        .search-input:focus {
            background: rgba(200, 200, 200, 0.25);
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.2);
        }

        .search-input::placeholder {
            color: rgba(255, 255, 255, 0.6);
        }

        /* games grid */
        .games-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
            gap: 20px;
            padding: 10px 0;
        }

        .game-card {
            background: var(--card-bg);
            border-radius: 20px;
            overflow: hidden;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            text-decoration: none;
            color: white;
            position: relative;
            aspect-ratio: 1;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        .game-card:hover {
            transform: scale(1.08);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.4);
            z-index: 10;
        }

        .game-card img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.4s ease;
        }

        .game-card:hover img {
            transform: scale(1.1);
        }

        .game-card-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            padding: 20px 15px;
            background: linear-gradient(transparent, rgba(0, 0, 0, 0.9));
            opacity: 0;
            transform: translateY(20px);
            transition: all 0.4s ease;
        }

        .game-card:hover .game-card-overlay {
            opacity: 1;
            transform: translateY(0);
        }

        .game-card-title {
            font-weight: 400;
            text-align: center;
            font-size: 1rem;
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.8);
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        /* glow effect on hover */
        .game-card::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: linear-gradient(45deg, rgba(255, 255, 255, 0.3), rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0.3));
            border-radius: 22px;
            opacity: 0;
            z-index: -1;
            transition: opacity 0.4s ease;
        }

        .game-card:hover::before {
            opacity: 1;
        }

        /* search results popup */
        .search-results {
            position: fixed;
            top: 80px;
            left: 50%;
            transform: translateX(-50%);
            width: 90%;
            max-width: 600px;
            max-height: 400px;
            overflow-y: auto;
            background: var(--card-bg);
            border-radius: 15px;
            padding: 15px;
            z-index: 1000;
            backdrop-filter: blur(10px);
            display: none;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.4);
        }

        .search-results.active {
            display: block;
            animation: slideDown 0.3s ease-out;
        }

        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateX(-50%) translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateX(-50%) translateY(0);
            }
        }

        .search-result-item {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 10px;
            border-radius: 10px;
            cursor: pointer;
            transition: background 0.3s ease;
            text-decoration: none;
            color: white;
        }

        .search-result-item:hover {
            background: rgba(255, 255, 255, 0.1);
        }

        .search-result-item img {
            width: 60px;
            height: 60px;
            object-fit: cover;
            border-radius: 10px;
        }

        .search-result-item .info {
            flex: 1;
        }

        .search-result-item .name {
            font-weight: 400;
            font-size: 1rem;
            margin-bottom: 5px;
        }

        .search-result-item .category {
            font-size: 0.8rem;
            color: rgba(255, 255, 255, 0.6);
        }

        .no-results {
            text-align: center;
            padding: 20px;
            color: rgba(255, 255, 255, 0.5);
            font-size: 1rem;
        }

        /* responsive */
        @media (max-width: 768px) {
            .sidebar {
                left: 10px;
                right: 10px;
                top: auto;
                bottom: 10px;
                width: auto;
                height: 70px;
                transform: none;
                flex-direction: row;
            }

            .page {
                padding-left: 20px;
                padding-bottom: 100px;
            }

            .games-grid {
                grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
                gap: 15px;
            }

            .search-results {
                width: 95%;
                top: 70px;
            }
        }
    </style>
<script src="https://sites.super.myninja.ai/_assets/ninja-daytona-script.js"></script>
</head>
<body>
    <div class="grainy-bg"></div>
    <div class="particles" id="particles"></div>
    <div class="mouse-shadow" id="mouseShadow"></div>

    <!-- main container -->
    <div class="main-container">
        <!-- sidebar -->
        <nav class="sidebar">
            <a href="https://twentyfolds.github.io/veinte/home" class="sidebar-icon" title="home">
                <i class="fas fa-home"></i>
            </a>
            <a href="/veinte/pages/g" class="sidebar-icon active" title="games">
                <i class="fas fa-gamepad"></i>
            </a>
            <a href="/veinte/pages/p" class="sidebar-icon" title="proxy">
                <i class="fas fa-search"></i>
            </a>
            <a href="/veinte/pages/t" class="sidebar-icon" title="tools">
                <i class="fas fa-tools"></i>
            </a>
            <a href="/veinte/pages/s" class="sidebar-icon" title="settings">
                <i class="fas fa-cog"></i>
            </a>
        </nav>

        <!-- games page -->
        <div class="page">
            <div class="search-container">
                <input type="text" class="search-input" id="searchInput" placeholder="search games..." onkeyup="searchgames()">
            </div>
            
            <!-- Search results popup -->
            <div class="search-results" id="searchResults"></div>
            
            <div class="games-grid" id="gamesGrid">
                <!-- games populated by js -->
            </div>
        </div>
    </div>

    <script>
        // all games data
        const games = [
            { id: 'game1', name: 'super game', img: 'https://via.placeholder.com/180x180/1a1a1a/ffffff?text=super+game', category: 'popular' },
            { id: 'game2', name: 'mega runner', img: 'https://via.placeholder.com/180x180/2a2a2a/ffffff?text=mega+runner', category: 'popular' },
            { id: 'game3', name: 'action blast', img: 'https://via.placeholder.com/180x180/3a3a3a/ffffff?text=action+blast', category: 'popular' },
            { id: 'game4', name: 'speed racer', img: 'https://via.placeholder.com/180x180/4a4a4a/ffffff?text=speed+racer', category: 'popular' },
            { id: 'game5', name: 'puzzle master', img: 'https://via.placeholder.com/180x180/5a5a5a/ffffff?text=puzzle+master', category: 'popular' },
            { id: 'game6', name: 'space quest', img: 'https://via.placeholder.com/180x180/6a6a6a/ffffff?text=space+quest', category: 'popular' },
            { id: 'game7', name: 'sky fighter', img: 'https://via.placeholder.com/180x180/7a7a7a/ffffff?text=sky+fighter', category: 'popular' },
            { id: 'game8', name: 'candy crush', img: 'https://via.placeholder.com/180x180/8a8a8a/ffffff?text=candy+crush', category: 'popular' },
            { id: 'action1', name: 'battle royale', img: 'https://via.placeholder.com/180x180/ff0000/ffffff?text=battle+royale', category: 'action' },
            { id: 'action2', name: 'fighter z', img: 'https://via.placeholder.com/180x180/ff3300/ffffff?text=fighter+z', category: 'action' },
            { id: 'action3', name: 'war zone', img: 'https://via.placeholder.com/180x180/ff6600/ffffff?text=war+zone', category: 'action' },
            { id: 'action4', name: 'combat elite', img: 'https://via.placeholder.com/180x180/ff9900/ffffff?text=combat+elite', category: 'action' },
            { id: 'action5', name: 'strike force', img: 'https://via.placeholder.com/180x180/ffcc00/ffffff?text=strike+force', category: 'action' },
            { id: 'action6', name: 'ninja attack', img: 'https://via.placeholder.com/180x180/ffff00/ffffff?text=ninja+attack', category: 'action' },
            { id: 'action7', name: 'sniper elite', img: 'https://via.placeholder.com/180x180/ff6600/ffffff?text=sniper+elite', category: 'action' },
            { id: 'action8', name: 'tank battle', img: 'https://via.placeholder.com/180x180/ff3300/ffffff?text=tank+battle', category: 'action' },
            { id: 'horror1', name: 'dark mansion', img: 'https://via.placeholder.com/180x180/4a0000/ffffff?text=dark+mansion', category: 'horror' },
            { id: 'horror2', name: 'creepy house', img: 'https://via.placeholder.com/180x180/660000/ffffff?text=creepy+house', category: 'horror' },
            { id: 'horror3', name: 'zombie apocalypse', img: 'https://via.placeholder.com/180x180/800000/ffffff?text=zombie+apocalypse', category: 'horror' },
            { id: 'horror4', name: 'ghost town', img: 'https://via.placeholder.com/180x180/990000/ffffff?text=ghost+town', category: 'horror' },
            { id: 'horror5', name: 'nightmare', img: 'https://via.placeholder.com/180x180/b30000/ffffff?text=nightmare', category: 'horror' },
            { id: 'horror6', name: 'scary maze', img: 'https://via.placeholder.com/180x180/cc0000/ffffff?text=scary+maze', category: 'horror' },
            { id: 'horror7', name: 'evil clown', img: 'https://via.placeholder.com/180x180/800000/ffffff?text=evil+clown', category: 'horror' },
            { id: 'horror8', name: 'haunted forest', img: 'https://via.placeholder.com/180x180/660000/ffffff?text=haunted+forest', category: 'horror' },
            { id: 'running1', name: 'endless run', img: 'https://via.placeholder.com/180x180/00ff00/000000?text=endless+run', category: 'running' },
            { id: 'running2', name: 'temple dash', img: 'https://via.placeholder.com/180x180/00cc00/000000?text=temple+dash', category: 'running' },
            { id: 'running3', name: 'subway surf', img: 'https://via.placeholder.com/180x180/009900/000000?text=subway+surf', category: 'running' },
            { id: 'running4', name: 'parkour run', img: 'https://via.placeholder.com/180x180/006600/000000?text=parkour+run', category: 'running' },
            { id: 'running5', name: 'city runner', img: 'https://via.placeholder.com/180x180/003300/000000?text=city+runner', category: 'running' },
            { id: 'running6', name: 'sprint king', img: 'https://via.placeholder.com/180x180/001a00/000000?text=sprint+king', category: 'running' },
            { id: 'running7', name: 'road dash', img: 'https://via.placeholder.com/180x180/00cc00/000000?text=road+dash', category: 'running' },
            { id: 'running8', name: 'track stars', img: 'https://via.placeholder.com/180x180/009900/000000?text=track+stars', category: 'running' },
            { id: 'beauty1', name: 'makeup studio', img: 'https://via.placeholder.com/180x180/ff69b4/ffffff?text=makeup+studio', category: 'beauty' },
            { id: 'beauty2', name: 'dress up', img: 'https://via.placeholder.com/180x180/ff1493/ffffff?text=dress+up', category: 'beauty' },
            { id: 'beauty3', name: 'hair salon', img: 'https://via.placeholder.com/180x180/ff00ff/ffffff?text=hair+salon', category: 'beauty' },
            { id: 'beauty4', name: 'fashion queen', img: 'https://via.placeholder.com/180x180/da70d6/ffffff?text=fashion+queen', category: 'beauty' },
            { id: 'beauty5', name: 'nail art', img: 'https://via.placeholder.com/180x180/ba55d3/ffffff?text=nail+art', category: 'beauty' },
            { id: 'beauty6', name: 'spa day', img: 'https://via.placeholder.com/180x180/9932cc/ffffff?text=spa+day', category: 'beauty' },
            { id: 'beauty7', name: 'princess dress', img: 'https://via.placeholder.com/180x180/da70d6/ffffff?text=princess+dress', category: 'beauty' },
            { id: 'beauty8', name: 'jewelry maker', img: 'https://via.placeholder.com/180x180/ba55d3/ffffff?text=jewelry+maker', category: 'beauty' }
        ];

        // theme definitions with full color schemes
        const themes = {
            default: {
                primaryBg: '#000000',
                secondaryBg: '#1a1a1a',
                cardBg: 'rgba(255, 255, 255, 0.1)',
                inputBg: 'rgba(200, 200, 200, 0.15)',
                gradientStart: '#000000',
                gradientMiddle: '#1a1a1a',
                gradientEnd: '#000000'
            },
            blue: {
                primaryBg: '#0f1624',
                secondaryBg: '#1a2332',
                cardBg: 'rgba(100, 149, 237, 0.15)',
                inputBg: 'rgba(100, 149, 237, 0.2)',
                gradientStart: '#0f1624',
                gradientMiddle: '#1a2332',
                gradientEnd: '#162a4a'
            },
            pink: {
                primaryBg: '#1a0f14',
                secondaryBg: '#2d1a2e',
                cardBg: 'rgba(219, 112, 147, 0.15)',
                inputBg: 'rgba(219, 112, 147, 0.2)',
                gradientStart: '#1a0f14',
                gradientMiddle: '#2d1a2e',
                gradientEnd: '#3e1621'
            },
            green: {
                primaryBg: '#0f1a0f',
                secondaryBg: '#1a2e1a',
                cardBg: 'rgba(60, 179, 113, 0.15)',
                inputBg: 'rgba(60, 179, 113, 0.2)',
                gradientStart: '#0f1a0f',
                gradientMiddle: '#1a2e1a',
                gradientEnd: '#163e1c'
            },
            purple: {
                primaryBg: '#1a0f24',
                secondaryBg: '#2a1a2e',
                cardBg: 'rgba(147, 112, 219, 0.15)',
                inputBg: 'rgba(147, 112, 219, 0.2)',
                gradientStart: '#1a0f24',
                gradientMiddle: '#2a1a2e',
                gradientEnd: '#3e1636'
            }
        };

        // apply saved theme
        function applysavedtheme() {
            const savedtheme = localStorage.getItem('veintetheme');
            if (savedtheme) {
                applytheme(savedtheme);
            }
        }

        function applytheme(themeName) {
            const theme = themes[themeName];
            if (!theme) return;

            const root = document.documentElement;
            
            // Update all CSS variables
            root.style.setProperty('--primary-bg', theme.primaryBg);
            root.style.setProperty('--secondary-bg', theme.secondaryBg);
            root.style.setProperty('--card-bg', theme.cardBg);
            root.style.setProperty('--input-bg', theme.inputBg);
            root.style.setProperty('--gradient-start', theme.gradientStart);
            root.style.setProperty('--gradient-middle', theme.gradientMiddle);
            root.style.setProperty('--gradient-end', theme.gradientEnd);

            // save theme to localStorage
            localStorage.setItem('veintetheme', themeName);
        }

        // populate games grid
        function populategames() {
            const grid = document.getElementById('gamesGrid');
            grid.innerHTML = games.map(game => `
                <a href="play.html?game=${game.id}" class="game-card">
                    <img src="${game.img}" alt="${game.name}">
                    <div class="game-card-overlay">
                        <div class="game-card-title">${game.name}</div>
                    </div>
                </a>
            `).join('');
        }

        // search games with popup
        function searchgames() {
            const searchterm = document.getElementById('searchInput').value.toLowerCase().trim();
            const searchresults = document.getElementById('searchResults');

            if (searchterm === '') {
                searchresults.classList.remove('active');
                searchresults.innerHTML = '';
                return;
            }

            const filteredgames = games.filter(game => 
                game.name.toLowerCase().includes(searchterm)
            );

            if (filteredgames.length === 0) {
                searchresults.innerHTML = '<div class="no-results">no games found</div>';
            } else {
                searchresults.innerHTML = filteredgames.map(game => `
                    <a href="play.html?game=${game.id}" class="search-result-item">
                        <img src="${game.img}" alt="${game.name}">
                        <div class="info">
                            <div class="name">${game.name}</div>
                            <div class="category">${game.category}</div>
                        </div>
                    </a>
                `).join('');
            }

            searchresults.classList.add('active');
        }

        // close search results when clicking outside
        document.addEventListener('click', (e) => {
            const searchInput = document.getElementById('searchInput');
            const searchResults = document.getElementById('searchResults');
            
            if (!searchInput.contains(e.target) && !searchResults.contains(e.target)) {
                searchResults.classList.remove('active');
            }
        });

        // initialize particles
        function createparticles() {
            const particlescontainer = document.getElementById('particles');
            if (!particlescontainer) return;
            
            for (let i = 0; i < 50; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.style.top = Math.random() * 100 + '%';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 20 + 's';
                particle.style.animationDuration = (15 + Math.random() * 10) + 's';
                particlescontainer.appendChild(particle);
            }
        }

        // mouse shadow follower
        document.addEventListener('mousemove', (e) => {
            const shadow = document.getElementById('mouseShadow');
            if (shadow) {
                shadow.style.left = e.clientX + 'px';
                shadow.style.top = e.clientY + 'px';
            }
        });

        // initialize on load
        window.addEventListener('load', () => {
            applysavedtheme();
            createparticles();
            populategames();
        });
    </script>
</body>
</html> make the game covers the same round rectangle and add the tags for like fire or fun etc. and make the round rectangles bigger than the short square ones. p.html <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>veinte - browser</title>
    <link rel="icon" type="image/png" href="https://cdn.jsdelivr.net/gh/twentyfolds/veinte@main/img/VeinteLogo.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@200;300;400&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link id="favicon" rel="icon" href="" type="image/x-icon">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
            font-weight: 200;
        }

        :root {
            --primary-bg: #000000;
            --secondary-bg: #1a1a1a;
            --text-color: #ffffff;
            --sidebar-bg: rgba(255, 255, 255, 0.1);
            --card-bg: rgba(255, 255, 255, 0.1);
            --input-bg: rgba(200, 200, 200, 0.15);
            --gradient-start: #000000;
            --gradient-middle: #1a1a1a;
            --gradient-end: #000000;
        }

        body {
            background: var(--primary-bg);
            color: var(--text-color);
            overflow: hidden;
            min-height: 100vh;
        }

        /* grainy gradient background - black only */
        .grainy-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, var(--gradient-start) 0%, var(--gradient-middle) 50%, var(--gradient-end) 100%);
            z-index: -2;
            transition: background 0.5s ease;
        }

        .grainy-bg::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
            opacity: 0.05;
            pointer-events: none;
        }

        /* particle background */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            overflow: hidden;
        }

        .particle {
            position: absolute;
            width: 3px;
            height: 3px;
            background: rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            animation: moveRight 20s linear infinite;
        }

        @keyframes moveRight {
            0% {
                transform: translateX(-100px);
            }
            100% {
                transform: translateX(calc(100vw + 100px));
            }
        }

        /* mouse shadow follower */
        .mouse-shadow {
            position: fixed;
            width: 200px;
            height: 200px;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.15) 0%, transparent 70%);
            border-radius: 50%;
            pointer-events: none;
            transform: translate(-50%, -50%);
            z-index: 9999;
            mix-blend-mode: screen;
        }

        /* main container */
        .main-container {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        /* sidebar */
        .sidebar {
            position: fixed;
            left: 20px;
            top: 50%;
            transform: translateY(-50%);
            width: 70px;
            background: var(--sidebar-bg);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 30px;
            padding: 30px 0;
            z-index: 100;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
        }

        .sidebar-icon {
            color: white;
            font-size: 1.5rem;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 50px;
            height: 50px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 12px;
            text-decoration: none;
        }

        .sidebar-icon:hover, .sidebar-icon.active {
            background: rgba(255, 255, 255, 0.2);
            transform: scale(1.1);
        }

        /* browser container */
        .browser-container {
            flex: 1;
            display: flex;
            flex-direction: column;
            padding-left: 110px;
            padding-top: 10px;
        }

        /* tab bar */
        .tab-bar {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 10px 15px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 15px 15px 0 0;
            margin-right: 20px;
            overflow-x: auto;
            scrollbar-width: none;
        }

        .tab-bar::-webkit-scrollbar {
            display: none;
        }

        .tab {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 10px 20px;
            background: var(--card-bg);
            border-radius: 10px 10px 0 0;
            cursor: pointer;
            transition: all 0.3s ease;
            min-width: 150px;
            max-width: 250px;
            backdrop-filter: blur(10px);
            animation: tabSlide 0.3s ease;
        }

        @keyframes tabSlide {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .tab.active {
            background: rgba(255, 255, 255, 0.15);
            border-bottom: 2px solid rgba(255, 255, 255, 0.3);
        }

        .tab:hover {
            background: rgba(255, 255, 255, 0.12);
        }

        .tab-icon {
            font-size: 0.8rem;
            opacity: 0.8;
        }

        .tab-title {
            flex: 1;
            font-size: 0.9rem;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            font-weight: 200;
        }

        .tab-close {
            width: 20px;
            height: 20px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.7rem;
            opacity: 0.6;
            transition: all 0.2s ease;
        }

        .tab-close:hover {
            background: rgba(255, 255, 255, 0.2);
            opacity: 1;
        }

        .new-tab-btn {
            width: 35px;
            height: 35px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            border: none;
            color: white;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
        }

        .new-tab-btn:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: rotate(90deg);
        }

        /* address bar */
        .address-bar-container {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 10px 15px;
            background: rgba(255, 255, 255, 0.08);
            margin-right: 20px;
            border-radius: 0;
        }

        .nav-btn {
            width: 35px;
            height: 35px;
            border-radius: 50%;
            background: transparent;
            border: none;
            color: rgba(255, 255, 255, 0.6);
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.9rem;
        }

        .nav-btn:hover:not(:disabled) {
            background: rgba(255, 255, 255, 0.1);
            color: white;
        }

        .nav-btn:disabled {
            opacity: 0.3;
            cursor: not-allowed;
        }

        .address-bar {
            flex: 1;
            padding: 10px 20px;
            border: none;
            border-radius: 20px;
            background: var(--input-bg);
            color: white;
            outline: none;
            font-family: 'Poppins', sans-serif;
            font-weight: 200;
            font-size: 0.9rem;
            transition: all 0.3s ease;
        }

        .address-bar:focus {
            background: rgba(200, 200, 200, 0.25);
            box-shadow: 0 0 15px rgba(255, 255, 255, 0.1);
        }

        .address-bar::placeholder {
            color: rgba(255, 255, 255, 0.5);
        }

        .search-btn {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            border: none;
            color: white;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .search-btn:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: scale(1.05);
        }

        /* browser content */
        .browser-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            background: transparent;
            margin-right: 20px;
            border-radius: 0 0 15px 15px;
            overflow: hidden;
            height: calc(100vh - 140px);
        }

        .iframe-container {
            flex: 1;
            width: 100%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: transparent;
        }

        iframe {
            width: 100%;
            height: 100%;
            border: none;
            background: white;
            border-radius: 0 0 15px 15px;
        }

        /* new tab page - simplified design */
        .new-tab-page {
            flex: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background: transparent;
            color: white;
            padding: 40px;
            width: 100%;
            height: 100%;
        }

        .new-tab-title {
            font-size: 5rem;
            font-weight: 200;
            margin-bottom: 50px;
            letter-spacing: 5px;
            text-shadow: 0 0 30px rgba(255, 255, 255, 0.3);
        }

        .new-tab-subtitle {
            font-size: 1rem;
            opacity: 0.6;
            margin-bottom: 40px;
            font-weight: 200;
        }

        .quick-links {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 20px;
            max-width: 600px;
            width: 100%;
        }

        .quick-link {
            background: rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 15px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s ease;
            backdrop-filter: blur(10px);
        }

        .quick-link:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: translateY(-5px);
        }

        .quick-link-icon {
            font-size: 2rem;
            margin-bottom: 10px;
        }

        .quick-link-title {
            font-size: 0.9rem;
            font-weight: 200;
        }

        /* loading indicator */
        .loading-bar {
            height: 3px;
            background: linear-gradient(90deg, #667eea, #764ba2);
            width: 0%;
            transition: width 0.3s ease;
        }

        .loading-bar.active {
            animation: loading 1.5s ease-in-out;
        }

        @keyframes loading {
            0% {
                width: 0%;
                opacity: 1;
            }
            50% {
                width: 70%;
                opacity: 1;
            }
            100% {
                width: 100%;
                opacity: 0;
            }
        }

        /* proxy status indicator */
        .proxy-status {
            position: fixed;
            top: 10px;
            right: 10px;
            padding: 8px 12px;
            border-radius: 8px;
            font-size: 0.8rem;
            z-index: 1000;
            transition: all 0.3s ease;
        }

        .proxy-status.online {
            background: rgba(76, 175, 80, 0.2);
            color: #4caf50;
            border: 1px solid rgba(76, 175, 80, 0.3);
        }

        .proxy-status.offline {
            background: rgba(244, 67, 54, 0.2);
            color: #f44336;
            border: 1px solid rgba(244, 67, 54, 0.3);
        }

        .proxy-status.loading {
            background: rgba(255, 193, 7, 0.2);
            color: #ffc107;
            border: 1px solid rgba(255, 193, 7, 0.3);
        }

        /* context menu */
        .context-menu {
            position: fixed;
            background: var(--card-bg);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 10px;
            padding: 5px 0;
            min-width: 180px;
            z-index: 10000;
            display: none;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.4);
        }

        .context-menu.active {
            display: block;
            animation: fadeIn 0.2s ease;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: scale(0.95);
            }
            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        .context-menu-item {
            padding: 10px 20px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 10px;
            transition: background 0.2s ease;
            font-size: 0.9rem;
        }

        .context-menu-item:hover {
            background: rgba(255, 255, 255, 0.1);
        }

        .context-menu-divider {
            height: 1px;
            background: rgba(255, 255, 255, 0.1);
            margin: 5px 0;
        }

        .context-menu-icon {
            width: 16px;
            text-align: center;
            opacity: 0.7;
        }

        /* responsive */
        @media (max-width: 768px) {
            .sidebar {
                left: 10px;
                right: 10px;
                top: auto;
                bottom: 10px;
                width: auto;
                height: 70px;
                transform: none;
                flex-direction: row;
            }

            .browser-container {
                padding-left: 20px;
                padding-bottom: 90px;
            }

            .tab-bar {
                padding: 8px 10px;
            }

            .tab {
                padding: 8px 15px;
                min-width: 120px;
            }

            .quick-links {
                grid-template-columns: repeat(2, 1fr);
                gap: 15px;
            }
        }
    </style>
<script src="https://sites.super.myninja.ai/_assets/ninja-daytona-script.js"></script>
<!-- Scramjet and proxy dependencies -->
<script src="https://cdn.jsdelivr.net/npm/@mercuryworkshop/bare-mux@0.1.2/dist/index.mjs" type="module"></script>
<script src="https://cdn.jsdelivr.net/npm/@mercuryworkshop/scramjet@1.2.2/scramjet.all.js"></script>
</head>
<body>
    <div class="grainy-bg"></div>
    <div class="particles" id="particles"></div>
    <div class="mouse-shadow" id="mouseShadow"></div>

    <!-- proxy status indicator -->
    <div class="proxy-status loading" id="proxyStatus">
        <i class="fas fa-spinner fa-spin"></i> Initializing proxy...
    </div>

    <!-- context menu -->
    <div class="context-menu" id="contextMenu">
        <div class="context-menu-item" onclick="navigateBack()">
            <span class="context-menu-icon"><i class="fas fa-arrow-left"></i></span>
            Back
        </div>
        <div class="context-menu-item" onclick="navigateForward()">
            <span class="context-menu-icon"><i class="fas fa-arrow-right"></i></span>
            Forward
        </div>
        <div class="context-menu-item" onclick="refreshPage()">
            <span class="context-menu-icon"><i class="fas fa-redo"></i></span>
            Refresh
        </div>
        <div class="context-menu-divider"></div>
        <div class="context-menu-item" onclick="copyUrl()">
            <span class="context-menu-icon"><i class="fas fa-copy"></i></span>
            Copy URL
        </div>
        <div class="context-menu-item" onclick="pasteUrl()">
            <span class="context-menu-icon"><i class="fas fa-paste"></i></span>
            Paste URL
        </div>
        <div class="context-menu-divider"></div>
        <div class="context-menu-item" onclick="openInNewTab()">
            <span class="context-menu-icon"><i class="fas fa-plus"></i></span>
            Open in New Tab
        </div>
        <div class="context-menu-item" onclick="duplicateTab()">
            <span class="context-menu-icon"><i class="fas fa-clone"></i></span>
            Duplicate Tab
        </div>
        <div class="context-menu-divider"></div>
        <div class="context-menu-item" onclick="bookmarkPage()">
            <span class="context-menu-icon"><i class="fas fa-bookmark"></i></span>
            Bookmark
        </div>
        <div class="context-menu-item" onclick="toggleFullScreen()">
            <span class="context-menu-icon"><i class="fas fa-expand"></i></span>
            Full Screen
        </div>
    </div>

    <!-- main container -->
    <div class="main-container">
        <!-- sidebar -->
        <nav class="sidebar">
            <a href="https://twentyfolds.github.io/veinte/home" class="sidebar-icon" title="home">
                <i class="fas fa-home"></i>
            </a>
            <a href="/veinte/pages/g" class="sidebar-icon" title="games">
                <i class="fas fa-gamepad"></i>
            </a>
            <a href="/veinte/pages/p" class="sidebar-icon active" title="browser">
                <i class="fas fa-globe"></i>
            </a>
            <a href="/veinte/pages/t" class="sidebar-icon" title="tools">
                <i class="fas fa-tools"></i>
            </a>
            <a href="/veinte/pages/s" class="sidebar-icon" title="settings">
                <i class="fas fa-cog"></i>
            </a>
        </nav>

        <!-- browser container -->
        <div class="browser-container">
            <!-- tab bar -->
            <div class="tab-bar" id="tabBar">
                <!-- tabs will be added here dynamically -->
            </div>

            <!-- address bar -->
            <div class="address-bar-container">
                <button class="nav-btn" id="backBtn" onclick="goBack()" title="Back">
                    <i class="fas fa-arrow-left"></i>
                </button>
                <button class="nav-btn" id="forwardBtn" onclick="goForward()" title="Forward">
                    <i class="fas fa-arrow-right"></i>
                </button>
                <button class="nav-btn" id="refreshBtn" onclick="refreshPage()" title="Refresh">
                    <i class="fas fa-redo"></i>
                </button>
                <input type="text" class="address-bar" id="addressBar" placeholder="Search or enter URL" onkeypress="handleAddressBar(event)">
                <button class="search-btn" onclick="navigateToUrl()" title="Go">
                    <i class="fas fa-arrow-right"></i>
                </button>
            </div>

            <!-- loading bar -->
            <div class="loading-bar" id="loadingBar"></div>

            <!-- browser content -->
            <div class="browser-content">
                <div class="iframe-container" id="iframeContainer">
                    <!-- iframe or new tab page will be shown here -->
                </div>
            </div>
        </div>
    </div>

    <script>
        // ==================== THEME CHANGER ====================
        // theme definitions with full color schemes
        const themes = {
            default: {
                primaryBg: '#000000',
                secondaryBg: '#1a1a1a',
                cardBg: 'rgba(255, 255, 255, 0.1)',
                inputBg: 'rgba(200, 200, 200, 0.15)',
                gradientStart: '#000000',
                gradientMiddle: '#1a1a1a',
                gradientEnd: '#000000'
            },
            blue: {
                primaryBg: '#0f1624',
                secondaryBg: '#1a2332',
                cardBg: 'rgba(100, 149, 237, 0.15)',
                inputBg: 'rgba(100, 149, 237, 0.2)',
                gradientStart: '#0f1624',
                gradientMiddle: '#1a2332',
                gradientEnd: '#162a4a'
            },
            pink: {
                primaryBg: '#1a0f14',
                secondaryBg: '#2d1a2e',
                cardBg: 'rgba(219, 112, 147, 0.15)',
                inputBg: 'rgba(219, 112, 147, 0.2)',
                gradientStart: '#1a0f14',
                gradientMiddle: '#2d1a2e',
                gradientEnd: '#3e1621'
            },
            green: {
                primaryBg: '#0f1a0f',
                secondaryBg: '#1a2e1a',
                cardBg: 'rgba(60, 179, 113, 0.15)',
                inputBg: 'rgba(60, 179, 113, 0.2)',
                gradientStart: '#0f1a0f',
                gradientMiddle: '#1a2e1a',
                gradientEnd: '#163e1c'
            },
            purple: {
                primaryBg: '#1a0f24',
                secondaryBg: '#2a1a2e',
                cardBg: 'rgba(147, 112, 219, 0.15)',
                inputBg: 'rgba(147, 112, 219, 0.2)',
                gradientStart: '#1a0f24',
                gradientMiddle: '#2a1a2e',
                gradientEnd: '#3e1636'
            }
        };

        // apply saved theme
        function applysavedtheme() {
            const savedtheme = localStorage.getItem('veintetheme');
            if (savedtheme) {
                applytheme(savedtheme);
            }
        }

        function applytheme(themeName) {
            const theme = themes[themeName];
            if (!theme) return;

            const root = document.documentElement;
            
            // Update all CSS variables
            root.style.setProperty('--primary-bg', theme.primaryBg);
            root.style.setProperty('--secondary-bg', theme.secondaryBg);
            root.style.setProperty('--card-bg', theme.cardBg);
            root.style.setProperty('--input-bg', theme.inputBg);
            root.style.setProperty('--gradient-start', theme.gradientStart);
            root.style.setProperty('--gradient-middle', theme.gradientMiddle);
            root.style.setProperty('--gradient-end', theme.gradientEnd);

            // save theme to localStorage
            localStorage.setItem('veintetheme', themeName);
        }

        // ==================== BROWSER FUNCTIONALITY WITH SCRAMJET ====================
        
        let tabs = [];
        let activeTabId = null;
        let tabCounter = 0;
        let scramjetInstance = null;
        let scramjetController = null;

        // Update proxy status indicator
        function updateProxyStatus(status, message) {
            const statusEl = document.getElementById('proxyStatus');
            if (!statusEl) return;
            
            statusEl.className = `proxy-status ${status}`;
            
            const icon = status === 'online' ? 'fa-check-circle' : 
                        status === 'offline' ? 'fa-times-circle' : 
                        'fa-spinner fa-spin';
            
            statusEl.innerHTML = `<i class="fas ${icon}"></i> ${message}`;
        }

        // Initialize Scramjet
        async function initScramjet() {
            updateProxyStatus('loading', 'Initializing proxy...');
            
            try {
                // Load Scramjet Controller
                const { ScramjetController } = $scramjetLoadController();
                scramjetController = new ScramjetController({
                    files: {
                        wasm: "https://cdn.jsdelivr.net/npm/@mercuryworkshop/scramjet@1.2.2/scramjet.wasm.wasm",
                        all: "https://cdn.jsdelivr.net/npm/@mercuryworkshop/scramjet@1.2.2/scramjet.all.js",
                        sync: "https://cdn.jsdelivr.net/npm/@mercuryworkshop/scramjet@1.2.2/scramjet.sync.js",
                    },
                    flags: {
                        rewriterLogs: false,
                        scramitize: false,
                        cleanErrors: true,
                        sourcemaps: true,
                    },
                });

                // Initialize Scramjet
                await scramjetController.init();
                updateProxyStatus('loading', 'Registering service worker...');
                
                // Register service worker
                if ('serviceWorker' in navigator) {
                    try {
                        await navigator.serviceWorker.register('./sw.js');
                        console.log('Service Worker registered successfully');
                    } catch (swError) {
                        console.warn('Service Worker registration failed:', swError);
                        updateProxyStatus('offline', 'Service worker failed');
                    }
                }

                // Setup transport
                try {
                    updateProxyStatus('loading', 'Configuring transport...');
                    const connection = new BareMux.BareMuxConnection("https://cdn.jsdelivr.net/npm/@mercuryworkshop/bare-mux@0.1.2/dist/worker.js");
                    await connection.setTransport(
                        "https://cdn.jsdelivr.net/npm/@mercuryworkshop/epoxy-transport@0.0.15/dist/index.mjs",
                        [{ wisp: "wss://wisp.mercurywork.shop/" }]
                    );
                    console.log('Transport configured successfully');
                } catch (transportError) {
                    console.warn('Transport setup failed:', transportError);
                    updateProxyStatus('offline', 'Transport failed');
                }

                console.log('Scramjet initialized successfully');
                updateProxyStatus('online', 'Proxy connected');
                
                // Auto-hide status after 3 seconds if successful
                setTimeout(() => {
                    const statusEl = document.getElementById('proxyStatus');
                    if (statusEl && statusEl.classList.contains('online')) {
                        statusEl.style.opacity = '0';
                        setTimeout(() => {
                            if (statusEl) {
                                statusEl.style.display = 'none';
                            }
                        }, 300);
                    }
                }, 3000);
                
                return scramjetController;
            } catch (error) {
                console.error('Scramjet initialization failed:', error);
                updateProxyStatus('offline', 'Proxy failed');
                return null;
            }
        }

        // Create a new Scramjet frame for a tab
        function createScramjetFrame() {
            if (!scramjetController) {
                console.error('Scramjet controller not initialized');
                return null;
            }
            
            const frame = scramjetController.createFrame();
            frame.frame.style.width = '100%';
            frame.frame.style.height = '100%';
            frame.frame.style.border = 'none';
            frame.frame.style.background = 'white';
            frame.frame.style.borderRadius = '0 0 15px 15px';
            
            // Listen for URL changes from within the iframe
            frame.addEventListener('urlchange', (e) => {
                const tab = tabs.find(t => t.id === activeTabId);
                if (tab && e.url) {
                    tab.url = e.url;
                    tab.title = getDomainName(e.url);
                    renderTabs();
                    updateAddressBar();
                }
            });
            
            // Listen for frame events
            frame.frame.addEventListener('load', () => {
                hideLoading();
                updateNavButtons();
            });
            
            return frame;
        }

        // Initialize browser
        async function initBrowser() {
            scramjetController = await initScramjet();
            
            // Setup network monitoring
            setupNetworkMonitoring();
            
            // Try to restore saved tabs
            restoreTabs();
            
            // Create new tab if no tabs were restored
            if (tabs.length === 0) {
                createNewTab();
            }
            
            // Setup auto-save
            setupAutoSave();
            
            // Load saved search engine preference
            const savedEngine = localStorage.getItem('veinte_search_engine');
            if (savedEngine && searchEngines[savedEngine]) {
                currentSearchEngine = savedEngine;
            }
            
            // Load saved zoom level
            const savedZoom = localStorage.getItem('veinte_zoom_level');
            if (savedZoom) {
                zoomLevel = parseInt(savedZoom, 10);
            }
        }

        // Create new tab - Fixed to properly create new tabs
        function createNewTab(url = null) {
            const tabId = ++tabCounter;
            const tab = {
                id: tabId,
                title: 'New Tab',
                url: url || null,
                history: [],
                historyIndex: -1,
                frame: null
            };
            
            tabs.push(tab);
            switchToTab(tabId);
        }

        // Render tabs
        function renderTabs() {
            const tabBar = document.getElementById('tabBar');
            tabBar.innerHTML = '';
            
            tabs.forEach(tab => {
                const tabElement = document.createElement('div');
                tabElement.className = `tab ${tab.id === activeTabId ? 'active' : ''}`;
                tabElement.onclick = () => switchToTab(tab.id);
                tabElement.innerHTML = `
                    <span class="tab-icon"><i class="fas fa-globe"></i></span>
                    <span class="tab-title">${tab.title}</span>
                    <span class="tab-close" onclick="event.stopPropagation(); closeTab(${tab.id})">
                        <i class="fas fa-times"></i>
                    </span>
                `;
                tabBar.appendChild(tabElement);
            });
            
            // Add new tab button
            const newTabBtn = document.createElement('button');
            newTabBtn.className = 'new-tab-btn';
            newTabBtn.onclick = () => createNewTab();
            newTabBtn.innerHTML = '<i class="fas fa-plus"></i>';
            tabBar.appendChild(newTabBtn);
        }

        // Switch to tab
        function switchToTab(tabId) {
            activeTabId = tabId;
            renderTabs();
            updateBrowserContent();
        }

        // Close tab
        function closeTab(tabId) {
            const tabIndex = tabs.findIndex(t => t.id === tabId);
            if (tabIndex === -1) return;
            
            // Clean up frame if exists
            const tab = tabs[tabIndex];
            if (tab.frame && tab.frame.frame) {
                tab.frame.frame.remove();
            }
            
            tabs.splice(tabIndex, 1);
            
            if (tabs.length === 0) {
                createNewTab();
            } else if (activeTabId === tabId) {
                // Switch to the last tab if the active tab was closed
                const newIndex = Math.min(tabIndex, tabs.length - 1);
                activeTabId = tabs[newIndex].id;
                renderTabs();
                updateBrowserContent();
            } else {
                renderTabs();
            }
        }

        // Update browser content
        function updateBrowserContent() {
            const tab = tabs.find(t => t.id === activeTabId);
            const container = document.getElementById('iframeContainer');
            const addressBar = document.getElementById('addressBar');
            
            if (!tab) return;
            
            if (!tab.url || tab.url === null) {
                // Show new tab page - simplified design with just "veinte" and icons
                container.innerHTML = `
                    <div class="new-tab-page">
                        <h1 class="new-tab-title">veinte</h1>
                        
                        <div class="quick-links">
                            <div class="quick-link" onclick="navigateTo('https://www.tiktok.com')">
                                <div class="quick-link-icon"><i class="fab fa-tiktok"></i></div>
                                <div class="quick-link-title">TikTok</div>
                            </div>
                            <div class="quick-link" onclick="navigateTo('https://chatgpt.com')">
                                <div class="quick-link-icon"><i class="fas fa-robot"></i></div>
                                <div class="quick-link-title">ChatGPT</div>
                            </div>
                            <div class="quick-link" onclick="navigateTo('https://www.youtube.com')">
                                <div class="quick-link-icon"><i class="fab fa-youtube"></i></div>
                                <div class="quick-link-title">YouTube</div>
                            </div>
                            <div class="quick-link" onclick="navigateTo('https://twitter.com')">
                                <div class="quick-link-icon"><i class="fab fa-twitter"></i></div>
                                <div class="quick-link-title">Twitter</div>
                            </div>
                        </div>
                    </div>
                `;
                addressBar.value = '';
            } else {
                // Show iframe with Scramjet
                if (!tab.frame) {
                    tab.frame = createScramjetFrame();
                }
                
                if (tab.frame) {
                    container.innerHTML = '';
                    container.appendChild(tab.frame.frame);
                    tab.frame.go(tab.url);
                }
                
                addressBar.value = tab.url;
            }
            
            updateNavButtons();
        }

        // Navigate to URL
        function navigateTo(url) {
            const tab = tabs.find(t => t.id === activeTabId);
            if (!tab) return;
            
            // Ensure url is a string
            if (typeof url !== 'string') {
                url = String(url);
            }
            
            // Get safe URL
            url = getSafeUrl(url);
            
            // Validate URL
            if (!isValidUrl(url)) {
                showError('Invalid URL. Please enter a valid web address.');
                return;
            }
            
            // Update tab data
            tab.url = url;
            tab.title = getDomainName(url);
            
            // Add to history
            if (tab.historyIndex < tab.history.length - 1) {
                tab.history = tab.history.slice(0, tab.historyIndex + 1);
            }
            tab.history.push(url);
            tab.historyIndex = tab.history.length - 1;
            
            renderTabs();
            updateBrowserContent();
            showLoading();
        }

        // Get domain name from URL
        function getDomainName(url) {
            try {
                if (!url || typeof url !== 'string') {
                    return 'Page';
                }
                const urlObj = new URL(url);
                return urlObj.hostname.replace('www.', '');
            } catch {
                return 'Page';
            }
        }

        // Handle address bar input
        function handleAddressBar(event) {
            if (event.key === 'Enter') {
                navigateToUrl();
            }
        }

        // Navigate to URL from address bar
        function navigateToUrl() {
            const addressBar = document.getElementById('addressBar');
            const url = addressBar.value.trim();
            
            if (url) {
                navigateTo(url);
                // Blur the address bar after navigation
                addressBar.blur();
            }
        }

        // Copy current URL to clipboard
        async function copyUrl() {
            const tab = tabs.find(t => t.id === activeTabId);
            if (tab && tab.url) {
                try {
                    await navigator.clipboard.writeText(tab.url);
                    console.log('URL copied to clipboard');
                } catch (err) {
                    console.error('Failed to copy URL:', err);
                }
            }
        }

        // Paste URL from clipboard
        async function pasteUrl() {
            try {
                const url = await navigator.clipboard.readText();
                const addressBar = document.getElementById('addressBar');
                addressBar.value = url;
                addressBar.focus();
            } catch (err) {
                console.error('Failed to paste URL:', err);
            }
        }

        // Save current page to bookmarks (localStorage)
        function saveBookmark() {
            const tab = tabs.find(t => t.id === activeTabId);
            if (!tab || !tab.url) return;
            
            let bookmarks = JSON.parse(localStorage.getItem('veinte_bookmarks') || '[]');
            
            const bookmark = {
                id: Date.now(),
                url: tab.url,
                title: tab.title,
                date: new Date().toISOString()
            };
            
            // Check if already bookmarked
            const exists = bookmarks.find(b => b.url === tab.url);
            if (!exists) {
                bookmarks.push(bookmark);
                localStorage.setItem('veinte_bookmarks', JSON.stringify(bookmarks));
                console.log('Bookmark saved');
            }
        }

        // Load bookmarks from localStorage
        function loadBookmarks() {
            return JSON.parse(localStorage.getItem('veinte_bookmarks') || '[]');
        }

        // Get browsing history
        function getBrowsingHistory() {
            const tab = tabs.find(t => t.id === activeTabId);
            return tab ? tab.history : [];
        }

        // Clear browsing history for current tab
        function clearTabHistory() {
            const tab = tabs.find(t => t.id === activeTabId);
            if (!tab) return;
            
            tab.history = [tab.url].filter(Boolean);
            tab.historyIndex = tab.history.length - 1;
            updateNavButtons();
        }

        // Get tab statistics
        function getTabStats() {
            return {
                totalTabs: tabs.length,
                activeTabId: activeTabId,
                currentUrl: tabs.find(t => t.id === activeTabId)?.url,
                historyLength: tabs.find(t => t.id === activeTabId)?.history.length || 0
            };
        }

        // Search engines configuration
        const searchEngines = {
            google: 'https://www.google.com/search?q=',
            duckduckgo: 'https://duckduckgo.com/?q=',
            bing: 'https://www.bing.com/search?q=',
            yahoo: 'https://search.yahoo.com/search?p='
        };

        let currentSearchEngine = 'google';

        // Set search engine
        function setSearchEngine(engine) {
            if (searchEngines[engine]) {
                currentSearchEngine = engine;
                localStorage.setItem('veinte_search_engine', engine);
            }
        }

        // Get current search engine
        function getSearchEngine() {
            return localStorage.getItem('veinte_search_engine') || currentSearchEngine;
        }

        // Perform search
        function performSearch(query) {
            const engine = getSearchEngine();
            const searchUrl = searchEngines[engine] + encodeURIComponent(query);
            navigateTo(searchUrl);
        }

        // Update safe URL function to use custom search engine
        function getSafeUrl(input) {
            let url = input.trim();
            
            // Check if it's a search query
            if (!url.includes('.') && !url.startsWith('http')) {
                return searchEngines[getSearchEngine()] + encodeURIComponent(url);
            }
            
            // Add protocol if missing
            if (!url.startsWith('http://') && !url.startsWith('https://')) {
                url = 'https://' + url;
            }
            
            return url;
        }

        // Handle URL encoding
        function encodeUrl(url) {
            try {
                return encodeURIComponent(url);
            } catch (e) {
                return url;
            }
        }

        // Handle URL decoding
        function decodeUrl(encodedUrl) {
            try {
                return decodeURIComponent(encodedUrl);
            } catch (e) {
                return encodedUrl;
            }
        }

        // Get URL parameters
        function getUrlParameters(url) {
            try {
                const urlObj = new URL(url);
                const params = {};
                urlObj.searchParams.forEach((value, key) => {
                    params[key] = value;
                });
                return params;
            } catch (e) {
                return {};
            }
        }

        // Check if URL is using HTTPS
        function isSecureUrl(url) {
            try {
                return new URL(url).protocol === 'https:';
            } catch (e) {
                return false;
            }
        }

        // Get domain from URL
        function getDomain(url) {
            try {
                const urlObj = new URL(url);
                return urlObj.hostname;
            } catch (e) {
                return '';
            }
        }

        // Check if URL is same as current tab
        function isCurrentUrl(url) {
            const tab = tabs.find(t => t.id === activeTabId);
            return tab && tab.url === url;
        }

        // Reload all tabs
        function reloadAllTabs() {
            tabs.forEach(tab => {
                if (tab.frame && tab.url) {
                    tab.frame.reload();
                }
            });
        }

        // Close all tabs except current
        function closeOtherTabs() {
            const currentTab = tabs.find(t => t.id === activeTabId);
            if (!currentTab) return;
            
            tabs.forEach(tab => {
                if (tab.id !== activeTabId && tab.frame && tab.frame.frame) {
                    tab.frame.frame.remove();
                }
            });
            
            tabs = [currentTab];
            renderTabs();
        }

        // Close tabs to the right
        function closeTabsToRight() {
            const currentIndex = tabs.findIndex(t => t.id === activeTabId);
            if (currentIndex === -1) return;
            
            const tabsToClose = tabs.slice(currentIndex + 1);
            tabsToClose.forEach(tab => {
                if (tab.frame && tab.frame.frame) {
                    tab.frame.frame.remove();
                }
            });
            
            tabs = tabs.slice(0, currentIndex + 1);
            renderTabs();
        }

        // Close tabs to the left
        function closeTabsToLeft() {
            const currentIndex = tabs.findIndex(t => t.id === activeTabId);
            if (currentIndex <= 0) return;
            
            const tabsToClose = tabs.slice(0, currentIndex);
            tabsToClose.forEach(tab => {
                if (tab.frame && tab.frame.frame) {
                    tab.frame.frame.remove();
                }
            });
            
            tabs = tabs.slice(currentIndex);
            renderTabs();
        }

        // Export tabs data
        function exportTabs() {
            const tabsData = tabs.map(tab => ({
                id: tab.id,
                title: tab.title,
                url: tab.url,
                history: tab.history
            }));
            
            const dataStr = JSON.stringify(tabsData, null, 2);
            const dataBlob = new Blob([dataStr], { type: 'application/json' });
            const url = URL.createObjectURL(dataBlob);
            
            const link = document.createElement('a');
            link.href = url;
            link.download = 'veinte-tabs.json';
            link.click();
            
            URL.revokeObjectURL(url);
        }

        // Import tabs data
        function importTabs(file) {
            const reader = new FileReader();
            reader.onload = (e) => {
                try {
                    const tabsData = JSON.parse(e.target.result);
                    tabsData.forEach(tabData => {
                        const tab = {
                            id: ++tabCounter,
                            title: tabData.title || 'Imported Tab',
                            url: tabData.url || null,
                            history: tabData.history || [],
                            historyIndex: tabData.history ? tabData.history.length - 1 : -1,
                            frame: null
                        };
                        tabs.push(tab);
                    });
                    renderTabs();
                    console.log('Tabs imported successfully');
                } catch (err) {
                    console.error('Failed to import tabs:', err);
                }
            };
            reader.readAsText(file);
        }

        // Auto-save tabs to localStorage
        function autoSaveTabs() {
            const tabsData = tabs.map(tab => ({
                title: tab.title,
                url: tab.url,
                history: tab.history,
                historyIndex: tab.historyIndex
            }));
            localStorage.setItem('veinte_saved_tabs', JSON.stringify(tabsData));
        }

        // Restore tabs from localStorage
        function restoreTabs() {
            try {
                const savedTabs = JSON.parse(localStorage.getItem('veinte_saved_tabs') || '[]');
                if (savedTabs.length > 0) {
                    tabs = [];
                    savedTabs.forEach((tabData, index) => {
                        const tab = {
                            id: ++tabCounter,
                            title: tabData.title || 'Restored Tab',
                            url: tabData.url || null,
                            history: tabData.history || [],
                            historyIndex: tabData.historyIndex || -1,
                            frame: null
                        };
                        tabs.push(tab);
                    });
                    
                    // Set first tab as active
                    if (tabs.length > 0) {
                        activeTabId = tabs[0].id;
                    }
                    
                    renderTabs();
                    console.log('Tabs restored successfully');
                }
            } catch (err) {
                console.error('Failed to restore tabs:', err);
            }
        }

        // Monitor tab changes for auto-save
        function setupAutoSave() {
            // Save tabs every 30 seconds
            setInterval(autoSaveTabs, 30000);
            
            // Save tabs before page unload
            window.addEventListener('beforeunload', autoSaveTabs);
        }

        // Get favicon URL for a domain
        function getFaviconUrl(url) {
            try {
                const domain = getDomain(url);
                return `https://www.google.com/s2/favicons?domain=${domain}&sz=64`;
            } catch (e) {
                return '';
            }
        }

        // Open in external browser
        function openInExternalBrowser(url) {
            const tab = tabs.find(t => t.id === activeTabId);
            const targetUrl = url || tab?.url;
            if (targetUrl) {
                window.open(targetUrl, '_blank');
            }
        }

        // Print current page
        function printPage() {
            const tab = tabs.find(t => t.id === activeTabId);
            if (tab?.frame?.frame) {
                try {
                    tab.frame.frame.contentWindow.print();
                } catch (e) {
                    console.error('Failed to print:', e);
                }
            }
        }

        // Zoom functions
        let zoomLevel = 100;

        function zoomIn() {
            if (zoomLevel < 200) {
                zoomLevel += 10;
                applyZoom();
                saveZoomLevel();
            }
        }

        function zoomOut() {
            if (zoomLevel > 50) {
                zoomLevel -= 10;
                applyZoom();
                saveZoomLevel();
            }
        }

        function resetZoom() {
            zoomLevel = 100;
            applyZoom();
            saveZoomLevel();
        }

        function saveZoomLevel() {
            localStorage.setItem('veinte_zoom_level', zoomLevel.toString());
        }

        function applyZoom() {
            const tab = tabs.find(t => t.id === activeTabId);
            if (tab?.frame?.frame) {
                tab.frame.frame.style.transform = `scale(${zoomLevel / 100})`;
                tab.frame.frame.style.transformOrigin = 'top left';
            }
        }

        // Context menu functions
        function showContextMenu(x, y) {
            const contextMenu = document.getElementById('contextMenu');
            contextMenu.style.left = x + 'px';
            contextMenu.style.top = y + 'px';
            contextMenu.classList.add('active');
        }

        function hideContextMenu() {
            const contextMenu = document.getElementById('contextMenu');
            contextMenu.classList.remove('active');
        }

        function navigateBack() {
            goBack();
            hideContextMenu();
        }

        function navigateForward() {
            goForward();
            hideContextMenu();
        }

        function openInNewTab() {
            const tab = tabs.find(t => t.id === activeTabId);
            if (tab?.url) {
                createNewTab(tab.url);
            }
            hideContextMenu();
        }

        function bookmarkPage() {
            saveBookmark();
            hideContextMenu();
        }

        // Handle context menu on right-click
        document.addEventListener('contextmenu', (e) => {
            // Don't show context menu on input fields or iframes
            const target = e.target;
            const isInput = target.tagName === 'INPUT' || target.tagName === 'TEXTAREA' || target.isContentEditable;
            const isIframe = target.tagName === 'IFRAME';
            
            // Show custom context menu unless right-clicking on iframe content
            if (!isInput && !isIframe) {
                e.preventDefault();
                showContextMenu(e.clientX, e.clientY);
            }
        });

        // Hide context menu when clicking elsewhere
        document.addEventListener('click', (e) => {
            const contextMenu = document.getElementById('contextMenu');
            if (!contextMenu.contains(e.target)) {
                hideContextMenu();
            }
        });

        // Handle escape key to close context menu
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape') {
                hideContextMenu();
            }
        });

        // Error recovery for proxy failures
        function handleProxyError(error) {
            console.error('Proxy error:', error);
            showError('Connection failed. Please check your internet connection and try again.');
        }

        // Network status monitoring
        function setupNetworkMonitoring() {
            window.addEventListener('online', () => {
                updateProxyStatus('online', 'Back online');
                setTimeout(() => {
                    const statusEl = document.getElementById('proxyStatus');
                    if (statusEl) {
                        statusEl.style.opacity = '0';
                    }
                }, 2000);
            });

            window.addEventListener('offline', () => {
                updateProxyStatus('offline', 'Offline');
                const statusEl = document.getElementById('proxyStatus');
                if (statusEl) {
                    statusEl.style.opacity = '1';
                }
            });
        }

        // Get user agent
        function getUserAgent() {
            return navigator.userAgent;
        }

        // Check mobile device
        function isMobileDevice() {
            return /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(getUserAgent());
        }

        // Get device info
        function getDeviceInfo() {
            return {
                userAgent: getUserAgent(),
                isMobile: isMobileDevice(),
                platform: navigator.platform,
                language: navigator.language,
                screenWidth: window.screen.width,
                screenHeight: window.screen.height,
                viewportWidth: window.innerWidth,
                viewportHeight: window.innerHeight
            };
        }

        // Format date
        function formatDate(date) {
            return new Date(date).toLocaleString();
        }

        // Time since last visit
        function getTimeSince(date) {
            const now = new Date();
            const past = new Date(date);
            const diff = now - past;
            
            const seconds = Math.floor(diff / 1000);
            const minutes = Math.floor(seconds / 60);
            const hours = Math.floor(minutes / 60);
            const days = Math.floor(hours / 24);
            
            if (days > 0) return `${days} day${days > 1 ? 's' : ''} ago`;
            if (hours > 0) return `${hours} hour${hours > 1 ? 's' : ''} ago`;
            if (minutes > 0) return `${minutes} minute${minutes > 1 ? 's' : ''} ago`;
            return 'Just now';
        }

        // Get page info
        function getPageInfo() {
            const tab = tabs.find(t => t.id === activeTabId);
            if (!tab) return null;
            
            return {
                url: tab.url,
                title: tab.title,
                domain: getDomain(tab.url),
                favicon: getFaviconUrl(tab.url),
                secure: isSecureUrl(tab.url),
                historyLength: tab.history.length,
                historyIndex: tab.historyIndex
            };
        }

        // Console helper for debugging
        const veinteConsole = {
            log: (...args) => console.log('[Veinte]', ...args),
            error: (...args) => console.error('[Veinte]', ...args),
            warn: (...args) => console.warn('[Veinte]', ...args),
            info: (...args) => console.info('[Veinte]', ...args),
            
            tabs: () => console.table(tabs),
            activeTab: () => console.log(getPageInfo()),
            stats: () => console.log(getTabStats()),
            device: () => console.log(getDeviceInfo())
        };

        // Expose Veinte API for external use
        window.Veinte = {
            navigate: navigateTo,
            createTab: createNewTab,
            closeTab: closeTab,
            getTabs: () => tabs,
            getActiveTab: () => tabs.find(t => t.id === activeTabId),
            getHistory: getBrowsingHistory,
            getBookmarks: loadBookmarks,
            getSettings: () => ({
                theme: localStorage.getItem('veintetheme'),
                searchEngine: getSearchEngine(),
                zoom: zoomLevel
            }),
            console: veinteConsole
        };

        console.log('Veinte API loaded. Access via window.Veinte');

        // Go back
        function goBack() {
            const tab = tabs.find(t => t.id === activeTabId);
            if (!tab || tab.historyIndex <= 0) return;
            
            tab.historyIndex--;
            tab.url = tab.history[tab.historyIndex];
            tab.title = getDomainName(tab.url);
            
            renderTabs();
            updateBrowserContent();
            showLoading();
            
            // Use Scramjet's back method if available
            if (tab.frame && tab.frame.back) {
                setTimeout(() => tab.frame.back(), 100);
            }
        }

        // Go forward
        function goForward() {
            const tab = tabs.find(t => t.id === activeTabId);
            if (!tab || tab.historyIndex >= tab.history.length - 1) return;
            
            tab.historyIndex++;
            tab.url = tab.history[tab.historyIndex];
            tab.title = getDomainName(tab.url);
            
            renderTabs();
            updateBrowserContent();
            showLoading();
            
            // Use Scramjet's forward method if available
            if (tab.frame && tab.frame.forward) {
                setTimeout(() => tab.frame.forward(), 100);
            }
        }

        // Refresh page
        function refreshPage() {
            const tab = tabs.find(t => t.id === activeTabId);
            if (!tab || !tab.url) return;
            
            if (tab.frame) {
                tab.frame.reload();
            }
            showLoading();
        }

        // Toggle full-screen mode
        function toggleFullScreen() {
            const container = document.getElementById('iframeContainer');
            const browserContent = document.querySelector('.browser-content');
            
            if (!document.fullscreenElement) {
                if (container.requestFullscreen) {
                    container.requestFullscreen();
                } else if (container.webkitRequestFullscreen) {
                    container.webkitRequestFullscreen();
                } else if (container.msRequestFullscreen) {
                    container.msRequestFullscreen();
                }
            } else {
                if (document.exitFullscreen) {
                    document.exitFullscreen();
                } else if (document.webkitExitFullscreen) {
                    document.webkitExitFullscreen();
                } else if (document.msExitFullscreen) {
                    document.msExitFullscreen();
                }
            }
        }

        // Focus address bar
        function focusAddressBar() {
            document.getElementById('addressBar').focus();
            document.getElementById('addressBar').select();
        }

        // Get page title from iframe
        function getPageTitle() {
            const tab = tabs.find(t => t.id === activeTabId);
            if (tab && tab.frame && tab.frame.frame) {
                try {
                    return tab.frame.frame.contentDocument?.title || tab.title;
                } catch (e) {
                    return tab.title;
                }
            }
            return tab?.title || 'New Tab';
        }

        // Update navigation buttons
        function updateNavButtons() {
            const tab = tabs.find(t => t.id === activeTabId);
            const backBtn = document.getElementById('backBtn');
            const forwardBtn = document.getElementById('forwardBtn');
            
            backBtn.disabled = !tab || tab.historyIndex <= 0;
            forwardBtn.disabled = !tab || tab.historyIndex >= tab.history.length - 1;
        }

        // Show loading animation
        function showLoading() {
            const loadingBar = document.getElementById('loadingBar');
            loadingBar.classList.add('active');
        }

        // Hide loading animation
        function hideLoading() {
            const loadingBar = document.getElementById('loadingBar');
            loadingBar.classList.remove('active');
        }

        // Update address bar value
        function updateAddressBar() {
            const tab = tabs.find(t => t.id === activeTabId);
            const addressBar = document.getElementById('addressBar');
            
            if (tab && tab.url) {
                addressBar.value = tab.url;
            } else {
                addressBar.value = '';
            }
        }

        // Clear browser cache and cookies for a tab
        async function clearTabData(tabId) {
            const tab = tabs.find(t => t.id === tabId);
            if (!tab) return;
            
            // Remove the frame to clear data
            if (tab.frame && tab.frame.frame) {
                tab.frame.frame.remove();
                tab.frame = null;
            }
            
            // Recreate frame if URL exists
            if (tab.url) {
                tab.frame = createScramjetFrame();
                const container = document.getElementById('iframeContainer');
                container.innerHTML = '';
                container.appendChild(tab.frame.frame);
                tab.frame.go(tab.url);
            }
        }

        // Open URL in new tab
        function openInNewTab(url) {
            createNewTab(url);
        }

        // Duplicate current tab
        function duplicateTab() {
            const tab = tabs.find(t => t.id === activeTabId);
            if (tab && tab.url) {
                createNewTab(tab.url);
            }
        }

        // Validate URL format
        function isValidUrl(string) {
            try {
                new URL(string);
                return true;
            } catch (_) {
                return false;
            }
        }

        // Get safe URL (adds protocol if missing)
        function getSafeUrl(input) {
            let url = input.trim();
            
            // Check if it's a search query
            if (!url.includes('.') && !url.startsWith('http')) {
                return `https://www.google.com/search?q=${encodeURIComponent(url)}`;
            }
            
            // Add protocol if missing
            if (!url.startsWith('http://') && !url.startsWith('https://')) {
                url = 'https://' + url;
            }
            
            return url;
        }

        // Show error message
        function showError(message) {
            const container = document.getElementById('iframeContainer');
            container.innerHTML = `
                <div class="new-tab-page">
                    <h1 class="new-tab-title" style="color: #ff6b6b;">Error</h1>
                    <p style="opacity: 0.8; margin-bottom: 30px;">${message}</p>
                    <button onclick="navigateTo('https://www.google.com')" 
                            style="padding: 15px 30px; background: rgba(255,255,255,0.1); 
                                   border: 1px solid rgba(255,255,255,0.3); border-radius: 10px; 
                                   color: white; cursor: pointer; font-family: 'Poppins', sans-serif;">
                        Go to Google
                    </button>
                </div>
            `;
        }

        // Initialize particles
        function createparticles() {
            const particlescontainer = document.getElementById('particles');
            if (!particlescontainer) return;
            
            for (let i = 0; i < 50; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.style.top = Math.random() * 100 + '%';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 20 + 's';
                particle.style.animationDuration = (15 + Math.random() * 10) + 's';
                particlescontainer.appendChild(particle);
            }
        }

        // Mouse shadow follower
        document.addEventListener('mousemove', (e) => {
            const shadow = document.getElementById('mouseShadow');
            if (shadow) {
                shadow.style.left = e.clientX + 'px';
                shadow.style.top = e.clientY + 'px';
            }
        });

        // Keyboard shortcuts
        document.addEventListener('keydown', (e) => {
            // Check if user is typing in input fields
            const activeElement = document.activeElement;
            const isInputFocused = activeElement.tagName === 'INPUT' || 
                                  activeElement.tagName === 'TEXTAREA' ||
                                  activeElement.isContentEditable;
            
            // Skip shortcuts if input is focused (except for escape)
            if (isInputFocused && e.key !== 'Escape') {
                return;
            }
            
            // Ctrl/Cmd + T: New tab
            if ((e.ctrlKey || e.metaKey) && e.key === 't') {
                e.preventDefault();
                createNewTab();
            }
            
            // Ctrl/Cmd + W: Close tab
            if ((e.ctrlKey || e.metaKey) && e.key === 'w') {
                e.preventDefault();
                const tab = tabs.find(t => t.id === activeTabId);
                if (tab) {
                    closeTab(tab.id);
                }
            }
            
            // Ctrl/Cmd + L: Focus address bar
            if ((e.ctrlKey || e.metaKey) && e.key === 'l') {
                e.preventDefault();
                focusAddressBar();
            }
            
            // Ctrl/Cmd + R: Refresh
            if ((e.ctrlKey || e.metaKey) && e.key === 'r') {
                e.preventDefault();
                refreshPage();
            }
            
            // Alt + Left Arrow: Back
            if (e.altKey && e.key === 'ArrowLeft') {
                e.preventDefault();
                goBack();
            }
            
            // Alt + Right Arrow: Forward
            if (e.altKey && e.key === 'ArrowRight') {
                e.preventDefault();
                goForward();
            }
            
            // F11: Full screen
            if (e.key === 'F11') {
                e.preventDefault();
                toggleFullScreen();
            }
            
            // Escape: Focus address bar when not in input
            if (e.key === 'Escape' && !isInputFocused) {
                focusAddressBar();
            }
        });

        // Handle fullscreen change
        document.addEventListener('fullscreenchange', () => {
            if (!document.fullscreenElement) {
                const tab = tabs.find(t => t.id === activeTabId);
                if (tab && tab.url) {
                    updateBrowserContent();
                }
            }
        });

        // Handle visibility change (pause animations when tab not visible)
        document.addEventListener('visibilitychange', () => {
            if (document.hidden) {
                // Page is hidden - could pause heavy operations
            } else {
                // Page is visible again
                updateAddressBar();
            }
        });

        // Initialize on load
        window.addEventListener('load', () => {
            applysavedtheme();
            createparticles();
            initBrowser();
        });
    </script>
</body>
</html> t.html<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>veinte - tools</title>
    <link rel="icon" type="image/png" href="https://cdn.jsdelivr.net/gh/twentyfolds/veinte@main/img/VeinteLogo.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link id="favicon" rel="icon" href="" type="image/x-icon">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
            font-weight: 300;
        }

        :root {
            --primary-bg: #000000;
            --secondary-bg: #1a1a1a;
            --text-color: #ffffff;
            --sidebar-bg: rgba(255, 255, 255, 0.1);
            --card-bg: rgba(255, 255, 255, 0.1);
            --gradient-start: #000000;
            --gradient-middle: #1a1a1a;
            --gradient-end: #000000;
        }

        body {
            background: var(--primary-bg);
            color: var(--text-color);
            overflow-x: hidden;
            min-height: 100vh;
        }

        /* grainy gradient background */
        .grainy-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, var(--gradient-start) 0%, var(--gradient-middle) 50%, var(--gradient-end) 100%);
            z-index: -2;
            transition: background 0.5s ease;
        }

        .grainy-bg::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
            opacity: 0.05;
            pointer-events: none;
        }

        /* mouse shadow follower */
        .mouse-shadow {
            position: fixed;
            width: 200px;
            height: 200px;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.15) 0%, transparent 70%);
            border-radius: 50%;
            pointer-events: none;
            transform: translate(-50%, -50%);
            z-index: 9999;
            mix-blend-mode: screen;
        }

        /* particle background */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            overflow: hidden;
        }

        .particle {
            position: absolute;
            width: 3px;
            height: 3px;
            background: rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            animation: moveRight 20s linear infinite;
        }

        @keyframes moveRight {
            0% {
                transform: translateX(-100px);
            }
            100% {
                transform: translateX(calc(100vw + 100px));
            }
        }

        /* main container */
        .main-container {
            min-height: 100vh;
        }

        /* sidebar */
        .sidebar {
            position: fixed;
            left: 20px;
            top: 50%;
            transform: translateY(-50%);
            width: 70px;
            background: var(--sidebar-bg);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 30px;
            padding: 30px 0;
            z-index: 100;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
        }

        .sidebar-icon {
            color: white;
            font-size: 1.5rem;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 50px;
            height: 50px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 12px;
            text-decoration: none;
        }

        .sidebar-icon:hover, .sidebar-icon.active {
            background: rgba(255, 255, 255, 0.2);
            transform: scale(1.1);
        }

        /* page content */
        .page {
            padding-left: 110px;
            padding-right: 20px;
            padding-top: 20px;
            padding-bottom: 20px;
        }

        /* tools grid */
        .tools-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 20px;
            padding: 20px;
        }

        .tool-card {
            background: var(--card-bg);
            border-radius: 20px;
            padding: 30px;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 15px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            color: white;
            transition: background 0.5s ease;
        }

        .tool-card:hover {
            transform: scale(1.05);
            background: rgba(255, 255, 255, 0.15);
        }

        .tool-icon {
            font-size: 2.5rem;
            color: white;
        }

        .tool-name {
            font-weight: 300;
            text-align: center;
        }

        /* responsive */
        @media (max-width: 768px) {
            .sidebar {
                left: 10px;
                right: 10px;
                top: auto;
                bottom: 10px;
                width: auto;
                height: 70px;
                transform: none;
                flex-direction: row;
            }

            .page {
                padding-left: 20px;
                padding-bottom: 100px;
            }

            .tools-grid {
                grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
                gap: 15px;
            }
        }
    </style>
<script src="https://sites.super.myninja.ai/_assets/ninja-daytona-script.js"></script>
</head>
<body>
    <div class="grainy-bg"></div>
    <div class="particles" id="particles"></div>
    <div class="mouse-shadow" id="mouseShadow"></div>

    <!-- main container -->
    <div class="main-container">
        <!-- sidebar -->
        <nav class="sidebar">
            <a href="https://twentyfolds.github.io/veinte/home" class="sidebar-icon" title="home">
                <i class="fas fa-home"></i>
            </a>
            <a href="/veinte/pages/g" class="sidebar-icon active" title="games">
                <i class="fas fa-gamepad"></i>
            </a>
            <a href="/veinte/pages/p" class="sidebar-icon" title="proxy">
                <i class="fas fa-search"></i>
            </a>
            <a href="/veinte/pages/t" class="sidebar-icon" title="tools">
                <i class="fas fa-tools"></i>
            </a>
            <a href="/veinte/pages/s" class="sidebar-icon" title="settings">
                <i class="fas fa-cog"></i>
            </a>
        </nav>

        <!-- tools page -->
        <div class="page">
            <div class="tools-grid">
                <a href="play.html?tool=calculator" class="tool-card">
                    <i class="fas fa-calculator tool-icon"></i>
                    <span class="tool-name">calculator</span>
                </a>
                <a href="play.html?tool=notes" class="tool-card">
                    <i class="fas fa-sticky-note tool-icon"></i>
                    <span class="tool-name">notes</span>
                </a>
                <a href="play.html?tool=timer" class="tool-card">
                    <i class="fas fa-clock tool-icon"></i>
                    <span class="tool-name">timer</span>
                </a>
                <a href="play.html?tool=weather" class="tool-card">
                    <i class="fas fa-cloud-sun tool-icon"></i>
                    <span class="tool-name">weather</span>
                </a>
                <a href="play.html?tool=calendar" class="tool-card">
                    <i class="fas fa-calendar tool-icon"></i>
                    <span class="tool-name">calendar</span>
                </a>
            </div>
        </div>
    </div>

    <script>
        // theme definitions with full color schemes
        const themes = {
            default: {
                primaryBg: '#000000',
                secondaryBg: '#1a1a1a',
                cardBg: 'rgba(255, 255, 255, 0.1)',
                gradientStart: '#000000',
                gradientMiddle: '#1a1a1a',
                gradientEnd: '#000000'
            },
            blue: {
                primaryBg: '#0f1624',
                secondaryBg: '#1a2332',
                cardBg: 'rgba(100, 149, 237, 0.15)',
                gradientStart: '#0f1624',
                gradientMiddle: '#1a2332',
                gradientEnd: '#162a4a'
            },
            pink: {
                primaryBg: '#1a0f14',
                secondaryBg: '#2d1a2e',
                cardBg: 'rgba(219, 112, 147, 0.15)',
                gradientStart: '#1a0f14',
                gradientMiddle: '#2d1a2e',
                gradientEnd: '#3e1621'
            },
            green: {
                primaryBg: '#0f1a0f',
                secondaryBg: '#1a2e1a',
                cardBg: 'rgba(60, 179, 113, 0.15)',
                gradientStart: '#0f1a0f',
                gradientMiddle: '#1a2e1a',
                gradientEnd: '#163e1c'
            },
            purple: {
                primaryBg: '#1a0f24',
                secondaryBg: '#2a1a2e',
                cardBg: 'rgba(147, 112, 219, 0.15)',
                gradientStart: '#1a0f24',
                gradientMiddle: '#2a1a2e',
                gradientEnd: '#3e1636'
            }
        };

        // apply saved theme
        function applysavedtheme() {
            const savedtheme = localStorage.getItem('veintetheme');
            if (savedtheme) {
                applytheme(savedtheme);
            }
        }

        function applytheme(themeName) {
            const theme = themes[themeName];
            if (!theme) return;

            const root = document.documentElement;
            
            // Update all CSS variables
            root.style.setProperty('--primary-bg', theme.primaryBg);
            root.style.setProperty('--secondary-bg', theme.secondaryBg);
            root.style.setProperty('--card-bg', theme.cardBg);
            root.style.setProperty('--gradient-start', theme.gradientStart);
            root.style.setProperty('--gradient-middle', theme.gradientMiddle);
            root.style.setProperty('--gradient-end', theme.gradientEnd);

            // save theme to localStorage
            localStorage.setItem('veintetheme', themeName);
        }

        // initialize particles
        function createparticles() {
            const particlescontainer = document.getElementById('particles');
            if (!particlescontainer) return;
            
            for (let i = 0; i < 50; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.style.top = Math.random() * 100 + '%';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 20 + 's';
                particle.style.animationDuration = (15 + Math.random() * 10) + 's';
                particlescontainer.appendChild(particle);
            }
        }

        // mouse shadow follower
        document.addEventListener('mousemove', (e) => {
            const shadow = document.getElementById('mouseShadow');
            if (shadow) {
                shadow.style.left = e.clientX + 'px';
                shadow.style.top = e.clientY + 'px';
            }
        });

        // initialize on load
        window.addEventListener('load', () => {
            applysavedtheme();
            createparticles();
        });
    </script>
</body>
</html> make this theretool icons are round rectangles the same too as g.htnml, make the icons a image. 
